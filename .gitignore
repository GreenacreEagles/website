node_modules
dist
.astro
.vercel
.netlify
.wrangler
.deploy-tmp
.env
.env.*
!.env.example
*.log
.env.local

# Backup/export artifacts (may contain production data or secrets; never commit)
backups/
backups/local/
*.dump
*.sql.gz

# Local k6 load-test output (staging only; never run against production)
load-test-results/