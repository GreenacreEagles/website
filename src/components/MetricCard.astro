---
interface Props {
  label: string;
  value: string | number;
  detail?: string;
}

const { label, value, detail } = Astro.props;
---

<article class="surface p-4 sm:p-5">
  <p class="text-xs font-black uppercase tracking-[.12em] text-eagles-green">{label}</p>
  <p class="mt-2 text-3xl font-black text-eagles-ink sm:text-4xl">{value}</p>
  {detail && <p class="mt-2 text-sm leading-6 text-eagles-ink/60">{detail}</p>}
</article>
