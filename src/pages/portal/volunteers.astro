---
import { requireUser } from "@lib/auth/guards";

export const prerender = false;
const session = await requireUser(Astro);
if (!session) return Astro.redirect("/login/", 303);
return Astro.redirect("/portal/roles/#wwcc", 303);
---
