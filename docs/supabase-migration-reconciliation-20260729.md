# Supabase migration reconciliation — 2026-07-29

No production DDL, data changes, or migration-history repairs were made during this audit.

## Decision summary

- 14 local timestamps contain SQL equivalent to migrations recorded in production under later timestamps. These are the only repair candidates, after CLI authentication.
- 13 early portal migrations have extensive matching catalog objects, but data statements, grants, and function bodies cannot all be proven from catalog presence alone; they remain **unable to determine safely**.
- Three alternate-timestamp migrations differ from the recorded production SQL and remain **unable to determine safely**.
- Three later migrations are **partially applied manually**.
- The WWCC and member-portal workflow migrations are **not applied**.
- Later dependency chain: `20260727085028` → `20260728021848` → `20260728120000`.

## Statement-by-statement comparison

Each statement below is listed in local execution order. “Matching alternate migration” means the complete normalized local SQL matches the recorded production migration; therefore every listed statement is present and must not be rerun.

### 20260723124525_team_posts_polls_portal_cleanup

Classification: **unable to determine safely**. Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.

1. `insert into public.permissions (key, name, description) values ('team_posts.create', 'Create team posts', 'Create announcements and polls for assigned teams.'), ('team_posts.modera…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
2. `insert into public.role_permissions (role_id, permission_id) select r.id, p.id from public.roles r join public.permissions p on p.key in ('team_posts.create', 'team_posts.moderate'…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
3. `insert into public.role_permissions (role_id, permission_id) select r.id, p.id from public.roles r join public.permissions p on p.key = 'team_posts.create' where r.key in ('coach',…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
4. `create table if not exists public.team_posts ( id uuid primary key default gen_random_uuid(), team_id uuid not null references public.teams(id) on delete cascade, author_id uuid no…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
5. `create table if not exists public.team_post_reactions ( id uuid primary key default gen_random_uuid(), post_id uuid not null references public.team_posts(id) on delete cascade, use…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
6. `create table if not exists public.team_poll_options ( id uuid primary key default gen_random_uuid(), post_id uuid not null references public.team_posts(id) on delete cascade, label…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
7. `create table if not exists public.team_poll_responses ( id uuid primary key default gen_random_uuid(), post_id uuid not null references public.team_posts(id) on delete cascade, opt…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
8. `create table if not exists public.team_post_reads ( post_id uuid not null references public.team_posts(id) on delete cascade, user_id uuid not null references public.profiles(id) o…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
9. `create index if not exists team_posts_team_status_pinned_idx on public.team_posts (team_id, status, is_pinned desc, published_at desc);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
10. `create index if not exists team_reactions_post_idx on public.team_post_reactions (post_id);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
11. `create index if not exists team_poll_options_post_idx on public.team_poll_options (post_id, sort_order);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
12. `create index if not exists team_poll_responses_post_idx on public.team_poll_responses (post_id);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
13. `alter table public.team_posts enable row level security;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
14. `alter table public.team_post_reactions enable row level security;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
15. `alter table public.team_poll_options enable row level security;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
16. `alter table public.team_poll_responses enable row level security;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
17. `alter table public.team_post_reads enable row level security;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
18. `create or replace function app_private.can_access_team(target_team_id uuid) returns boolean language sql stable security definer set search_path = public, extensions as $$ select e…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
19. `revoke all on function app_private.can_access_team(uuid) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
20. `grant execute on function app_private.can_access_team(uuid) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
21. `create policy team_posts_accessible_read on public.team_posts for select to authenticated using (status <> 'draft' and app_private.can_access_team(team_id));` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
22. `create policy team_posts_authorised_insert on public.team_posts for insert to authenticated with check ( author_id = auth.uid() and ( app_private.has_permission('team_posts.create'…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
23. `create policy team_posts_author_moderator_update on public.team_posts for update to authenticated using ( author_id = auth.uid() or app_private.has_permission('team_posts.moderate'…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
24. `create policy team_post_reactions_accessible_read on public.team_post_reactions for select to authenticated using (exists (select 1 from public.team_posts tp where tp.id = post_id …` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
25. `create policy team_post_reactions_own_upsert on public.team_post_reactions for insert to authenticated with check (user_id = auth.uid() and exists (select 1 from public.team_posts …` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
26. `create policy team_post_reactions_own_update on public.team_post_reactions for update to authenticated using (user_id = auth.uid()) with check (user_id = auth.uid() and exists (sel…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
27. `create policy team_poll_options_accessible_read on public.team_poll_options for select to authenticated using (exists (select 1 from public.team_posts tp where tp.id = post_id and …` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
28. `create policy team_poll_options_authorised_insert on public.team_poll_options for insert to authenticated with check (exists (select 1 from public.team_posts tp where tp.id = post_…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
29. `create policy team_poll_responses_accessible_read on public.team_poll_responses for select to authenticated using ( user_id = auth.uid() or exists ( select 1 from public.team_posts…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
30. `create policy team_poll_responses_own_insert on public.team_poll_responses for insert to authenticated with check ( user_id = auth.uid() and exists ( select 1 from public.team_post…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
31. `create policy team_poll_responses_own_update on public.team_poll_responses for update to authenticated using (user_id = auth.uid()) with check ( user_id = auth.uid() and exists ( s…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
32. `create policy team_post_reads_own_manage on public.team_post_reads for all to authenticated using (user_id = auth.uid()) with check (user_id = auth.uid() and exists (select 1 from …` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
33. `grant select, insert, update on table public.team_posts to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
34. `grant select, insert, update on table public.team_post_reactions to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
35. `grant select, insert on table public.team_poll_options to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
36. `grant select, insert, update on table public.team_poll_responses to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
37. `grant select, insert, update on table public.team_post_reads to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
38. `create or replace function public.member_team_ids() returns table(team_id uuid, relationship text) language sql stable security invoker set search_path = public, extensions as $$ s…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
39. `grant execute on function public.member_team_ids() to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
40. `revoke execute on function public.request_role(uuid, uuid, uuid, text, text, text) from authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
41. `revoke execute on function public.withdraw_role_request(uuid, text) from authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
42. `revoke execute on function public.review_role_request(uuid, text, text, timestamptz, timestamptz) from authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
43. `drop policy if exists role_requests_create_own on public.role_requests;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
44. `drop policy if exists role_requests_review on public.role_requests;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
45. `drop policy if exists roles_requestable_read on public.roles;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
46. `drop policy if exists seasons_role_request_read on public.seasons;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
47. `drop policy if exists teams_role_request_read on public.teams;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
48. `comment on table public.role_requests is 'Deprecated historical access-request records. Member-driven role/team requests are disabled; administrators assign roles through Users.';` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)

### 20260723130432_family_wallet_controls

Classification: **unable to determine safely**. Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.

1. `insert into public.permissions (key, name, description) values ('families.invite', 'Invite family guardians', 'Invite another guardian into an existing family relationship.') on co…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
2. `insert into public.role_permissions (role_id, permission_id) select r.id, p.id from public.roles r join public.permissions p on p.key = 'families.invite' where r.key in ('super_adm…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
3. `create table if not exists public.family_relationship_invitations ( id uuid primary key default gen_random_uuid(), family_id uuid not null references public.families(id) on delete …` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
4. `create table if not exists public.family_voucher_assignments ( id uuid primary key default gen_random_uuid(), voucher_id uuid not null references public.voucher_issuances(id) on de…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
5. `alter table public.voucher_issuances add column if not exists assigned_by uuid references public.profiles(id) on delete set null, add column if not exists assigned_at timestamptz, …` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
6. `create index if not exists family_invitations_family_status_idx on public.family_relationship_invitations (family_id, status, created_at desc);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
7. `create index if not exists family_invitations_invited_user_idx on public.family_relationship_invitations (invited_user_id, status);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
8. `create index if not exists family_invitations_email_idx on public.family_relationship_invitations (invited_email, status);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
9. `create index if not exists family_voucher_assignments_family_idx on public.family_voucher_assignments (family_id, created_at desc);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
10. `create index if not exists voucher_issuances_family_beneficiary_idx on public.voucher_issuances (family_id, beneficiary_id, status);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
11. `alter table public.family_relationship_invitations enable row level security;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
12. `alter table public.family_voucher_assignments enable row level security;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
13. `create or replace function app_private.can_manage_family(target_family_id uuid) returns boolean language sql stable security definer set search_path = public, extensions as $$ sele…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
14. `create or replace function app_private.is_active_family_member(target_family_id uuid, target_user_id uuid) returns boolean language sql stable security definer set search_path = pu…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
15. `create or replace function app_private.can_assign_family_wallet_item(target_family_id uuid, target_child_id uuid) returns boolean language sql stable security definer set search_pa…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
16. `revoke all on function app_private.can_manage_family(uuid) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
17. `revoke all on function app_private.is_active_family_member(uuid, uuid) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
18. `revoke all on function app_private.can_assign_family_wallet_item(uuid, uuid) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
19. `grant execute on function app_private.can_manage_family(uuid) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
20. `grant execute on function app_private.is_active_family_member(uuid, uuid) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
21. `grant execute on function app_private.can_assign_family_wallet_item(uuid, uuid) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
22. `create policy family_invitations_related_read on public.family_relationship_invitations for select to authenticated using ( invited_user_id = auth.uid() or lower(invited_email) = l…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
23. `create policy family_invitations_manage_insert on public.family_relationship_invitations for insert to authenticated with check ( invited_by = auth.uid() and app_private.can_manage…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
24. `create policy family_invitations_related_update on public.family_relationship_invitations for update to authenticated using ( app_private.can_manage_family(family_id) or invited_us…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
25. `create policy family_voucher_assignments_related_read on public.family_voucher_assignments for select to authenticated using ( assigned_by = auth.uid() or from_user_id = auth.uid()…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
26. `drop policy if exists vouchers_owner_or_manager_read on public.voucher_issuances;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
27. `create policy vouchers_owner_family_or_manager_read on public.voucher_issuances for select to authenticated using ( beneficiary_id = auth.uid() or app_private.has_permission('cante…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
28. `drop policy if exists wallets_owner_or_admin_read on public.wallet_accounts;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
29. `create policy wallets_owner_family_or_admin_read on public.wallet_accounts for select to authenticated using ( owner_id = auth.uid() or app_private.has_permission('wallet.read') or…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
30. `drop policy if exists ledger_owner_or_admin_read on public.wallet_ledger_entries;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
31. `create policy ledger_owner_family_or_admin_read on public.wallet_ledger_entries for select to authenticated using ( app_private.has_permission('wallet.read') or exists ( select 1 f…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
32. `create or replace function public.invite_family_guardian( target_family_id uuid, invite_email text, invite_relationship text default 'guardian', invite_message text default null ) …` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
33. `create or replace function app_private.accept_family_invitation(target_invitation_id uuid) returns uuid language plpgsql security definer set search_path = public, extensions as $$…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
34. `create or replace function app_private.assign_voucher_to_family_member( target_voucher_id uuid, target_child_id uuid, assignment_note text default null ) returns uuid language plpg…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
35. `create or replace function public.accept_family_invitation(target_invitation_id uuid) returns uuid language sql security invoker set search_path = public, extensions as $$ select a…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
36. `create or replace function public.assign_voucher_to_family_member( target_voucher_id uuid, target_child_id uuid, assignment_note text default null ) returns uuid language sql secur…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
37. `revoke all on function public.invite_family_guardian(uuid, text, text, text) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
38. `revoke all on function public.accept_family_invitation(uuid) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
39. `revoke all on function public.assign_voucher_to_family_member(uuid, uuid, text) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
40. `revoke all on function app_private.accept_family_invitation(uuid) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
41. `revoke all on function app_private.assign_voucher_to_family_member(uuid, uuid, text) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
42. `grant execute on function app_private.accept_family_invitation(uuid) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
43. `grant execute on function app_private.assign_voucher_to_family_member(uuid, uuid, text) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
44. `grant execute on function public.invite_family_guardian(uuid, text, text, text) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
45. `grant execute on function public.accept_family_invitation(uuid) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
46. `grant execute on function public.assign_voucher_to_family_member(uuid, uuid, text) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
47. `grant select, insert, update on table public.family_relationship_invitations to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
48. `grant select on table public.family_voucher_assignments to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
49. `grant select on table public.family_voucher_assignments to service_role;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)

### 20260723131846_squad_staff_match_reports

Classification: **unable to determine safely**. Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.

1. `create or replace function app_private.can_manage_team_operations(target_team_id uuid) returns boolean language sql stable security definer set search_path = public, extensions as …` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
2. `revoke all on function app_private.can_manage_team_operations(uuid) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
3. `grant execute on function app_private.can_manage_team_operations(uuid) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
4. `drop policy if exists team_posts_authorised_insert on public.team_posts;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
5. `create policy team_posts_authorised_insert on public.team_posts for insert to authenticated with check ( author_id = auth.uid() and app_private.can_manage_team_operations(team_id) …` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
6. `drop policy if exists team_posts_author_moderator_update on public.team_posts;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
7. `create policy team_posts_author_moderator_update on public.team_posts for update to authenticated using ( (author_id = auth.uid() and app_private.can_manage_team_operations(team_id…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
8. `drop policy if exists match_reports_author_team_admin on public.match_reports;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
9. `create policy match_reports_author_team_admin on public.match_reports for select to authenticated using ( author_id = auth.uid() or app_private.can_manage_team_operations(team_id) …` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
10. `drop policy if exists match_reports_submit on public.match_reports;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
11. `create policy match_reports_submit on public.match_reports for insert to authenticated with check ( author_id = auth.uid() and app_private.can_manage_team_operations(team_id) );` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
12. `drop policy if exists match_reports_update_author_or_reviewer on public.match_reports;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
13. `create policy match_reports_update_author_or_reviewer on public.match_reports for update to authenticated using ( (author_id = auth.uid() and status in ('draft', 'changes_requested…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
14. `create index if not exists match_reports_team_status_created_idx on public.match_reports (team_id, status, created_at desc);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
15. `drop policy if exists team_staff_read_team on public.team_staff;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
16. `create policy team_staff_read_team on public.team_staff for select to authenticated using ( user_id = auth.uid() or app_private.can_access_team(team_id) or app_private.has_permissi…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
17. `drop policy if exists team_players_read_team_staff on public.team_players;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
18. `create policy team_players_read_team_staff on public.team_players for select to authenticated using ( app_private.can_access_team(team_id) or app_private.has_permission('teams.mana…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)

### 20260723133842_canteen_fulfilment_operations

Classification: **unable to determine safely**. Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.

1. `alter table public.canteen_products add column if not exists fulfilment_type text not null default 'direct_order' check (fulfilment_type in ('direct_order', 'item_voucher')), add c…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
2. `alter table public.canteen_order_items add column if not exists fulfilment_type_snapshot text not null default 'direct_order' check (fulfilment_type_snapshot in ('direct_order', 'i…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
3. `alter table public.canteen_orders add column if not exists pickup_code text unique, add column if not exists pickup_token_hash text unique;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
4. `create index if not exists canteen_products_fulfilment_active_idx on public.canteen_products (fulfilment_type, is_active, is_sold_out, display_order);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
5. `create index if not exists canteen_orders_status_payment_created_idx on public.canteen_orders (order_status, payment_status, created_at);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
6. `create index if not exists canteen_order_items_order_fulfilment_idx on public.canteen_order_items (order_id, fulfilment_type_snapshot);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
7. `create or replace function app_private.issue_canteen_order_vouchers(target_order_id uuid) returns int language plpgsql security definer set search_path = public, extensions as $$ d…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
8. `create or replace function app_private.update_canteen_order_state( target_order_id uuid, target_order_status text default null, target_payment_status text default null, change_reas…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
9. `create or replace function app_private.create_canteen_order( target_product_id uuid, target_venue_id uuid default null, target_beneficiary_id uuid default null, order_quantity int …` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
10. `create or replace function public.create_canteen_order( target_product_id uuid, target_venue_id uuid default null, target_beneficiary_id uuid default null, order_quantity int defau…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
11. `create or replace function public.update_canteen_order_state( target_order_id uuid, target_order_status text default null, target_payment_status text default null, change_reason te…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
12. `revoke all on function app_private.issue_canteen_order_vouchers(uuid) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
13. `revoke all on function app_private.create_canteen_order(uuid, uuid, uuid, int, timestamptz, text) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
14. `revoke all on function public.create_canteen_order(uuid, uuid, uuid, int, timestamptz, text) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
15. `revoke all on function app_private.update_canteen_order_state(uuid, text, text, text) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
16. `revoke all on function public.update_canteen_order_state(uuid, text, text, text) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
17. `grant execute on function app_private.issue_canteen_order_vouchers(uuid) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
18. `grant execute on function app_private.create_canteen_order(uuid, uuid, uuid, int, timestamptz, text) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
19. `grant execute on function public.create_canteen_order(uuid, uuid, uuid, int, timestamptz, text) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
20. `grant execute on function app_private.update_canteen_order_state(uuid, text, text, text) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
21. `grant execute on function public.update_canteen_order_state(uuid, text, text, text) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)

### 20260723141504_merchandise_order_management

Classification: **unable to determine safely**. Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.

1. `create table if not exists public.merchandise_order_items ( id uuid primary key default gen_random_uuid(), order_id uuid not null references public.merchandise_orders(id) on delete…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
2. `alter table public.merchandise_orders add column if not exists paid_at timestamptz, add column if not exists stock_released_at timestamptz;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
3. `create table if not exists public.merchandise_order_status_history ( id uuid primary key default gen_random_uuid(), order_id uuid not null references public.merchandise_orders(id) …` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
4. `create index if not exists merchandise_order_items_order_idx on public.merchandise_order_items (order_id);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
5. `create index if not exists merchandise_order_items_variant_idx on public.merchandise_order_items (variant_id);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
6. `create index if not exists merchandise_orders_status_created_idx on public.merchandise_orders (status, created_at);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
7. `alter table public.merchandise_order_items enable row level security;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
8. `alter table public.merchandise_order_status_history enable row level security;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
9. `drop policy if exists merchandise_order_items_read_own_or_manager on public.merchandise_order_items;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
10. `create policy merchandise_order_items_read_own_or_manager on public.merchandise_order_items for select to authenticated using ( app_private.has_permission('merchandise.manage') or …` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
11. `drop policy if exists merchandise_order_history_manager_read on public.merchandise_order_status_history;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
12. `create policy merchandise_order_history_manager_read on public.merchandise_order_status_history for select to authenticated using (app_private.has_permission('merchandise.manage'))…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
13. `create or replace function app_private.create_merchandise_order( target_variant_id uuid, order_quantity int default 1, target_pickup_or_delivery text default 'pickup', target_notes…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
14. `create or replace function app_private.update_merchandise_order_state( target_order_id uuid, target_status text, change_reason text default null ) returns table ( order_id uuid, or…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
15. `create or replace function public.create_merchandise_order( target_variant_id uuid, order_quantity int default 1, target_pickup_or_delivery text default 'pickup', target_notes text…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
16. `create or replace function public.update_merchandise_order_state( target_order_id uuid, target_status text, change_reason text default null ) returns table ( order_id uuid, order_n…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
17. `revoke all on function app_private.create_merchandise_order(uuid, int, text, text) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
18. `revoke all on function public.create_merchandise_order(uuid, int, text, text) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
19. `revoke all on function app_private.update_merchandise_order_state(uuid, text, text) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
20. `revoke all on function public.update_merchandise_order_state(uuid, text, text) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
21. `grant execute on function app_private.create_merchandise_order(uuid, int, text, text) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
22. `grant execute on function public.create_merchandise_order(uuid, int, text, text) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
23. `grant execute on function app_private.update_merchandise_order_state(uuid, text, text) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
24. `grant execute on function public.update_merchandise_order_state(uuid, text, text) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
25. `grant select on public.merchandise_order_items to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
26. `grant select on public.merchandise_order_status_history to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
27. `grant select, insert, update, delete on public.merchandise_order_items to service_role;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
28. `grant select, insert, update, delete on public.merchandise_order_status_history to service_role;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)

### 20260723144236_voucher_reversal_read_policies

Classification: **unable to determine safely**. Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.

1. `drop policy if exists voucher_redemptions_staff_read on public.voucher_redemptions;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
2. `create policy voucher_redemptions_staff_read on public.voucher_redemptions for select to authenticated using ( redeemed_by = auth.uid() or app_private.has_permission('canteen.vouch…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
3. `drop policy if exists voucher_reversals_manager_read on public.voucher_reversals;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
4. `create policy voucher_reversals_manager_read on public.voucher_reversals for select to authenticated using ( app_private.has_permission('canteen.vouchers.manage') or app_private.ha…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
5. `grant select on public.voucher_redemptions to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
6. `grant select on public.voucher_reversals to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)

### 20260723165905_wallet_payment_operations

Classification: **unable to determine safely**. Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.

1. `alter table public.payments add column if not exists settled_at timestamptz, add column if not exists settled_by uuid references public.profiles(id) on delete set null;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
2. `create index if not exists payments_status_created_idx on public.payments (status, created_at);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
3. `create index if not exists payments_wallet_metadata_idx on public.payments ((metadata->>'wallet_account_id')) where metadata ? 'wallet_account_id';` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
4. `create unique index if not exists wallet_accounts_owner_user_unique on public.wallet_accounts (owner_id) where owner_id is not null and account_type = 'user';` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
5. `create unique index if not exists wallet_accounts_family_unique on public.wallet_accounts (family_id) where family_id is not null and account_type = 'family';` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
6. `create or replace function public.ensure_wallet_account( target_owner_id uuid default null, target_family_id uuid default null, target_account_type text default 'user' ) returns uu…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
7. `create or replace function app_private.wallet_balance_cents(wallet_id uuid) returns int language sql security definer set search_path = public, extensions as $$ select coalesce(( s…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
8. `create or replace function app_private.can_use_wallet(target_wallet_id uuid) returns boolean language sql security definer set search_path = public, extensions as $$ select exists …` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
9. `create or replace function public.create_wallet_top_up( target_wallet_id uuid, top_up_amount_cents int, provider text default 'manual', idempotency_key text default null ) returns …` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
10. `create or replace function public.settle_wallet_top_up( target_payment_id uuid, target_status text, provider_payment_id text default null, settlement_note text default null ) retur…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
11. `create or replace function public.adjust_wallet_balance( target_wallet_id uuid, amount_cents int, direction text, transaction_type text, description text, idempotency_key text defa…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
12. `create or replace function public.reverse_wallet_ledger_entry( target_entry_id uuid, reason text ) returns uuid language plpgsql security definer set search_path = public, extensio…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
13. `drop policy if exists payments_owner_or_finance_read on public.payments;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
14. `create policy payments_owner_wallet_or_finance_read on public.payments for select to authenticated using ( payer_id = auth.uid() or beneficiary_id = auth.uid() or app_private.has_p…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
15. `grant select on public.wallet_accounts to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
16. `grant select on public.wallet_ledger_entries to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
17. `grant select on public.wallet_balances to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
18. `grant select on public.payments to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
19. `revoke all on function public.ensure_wallet_account(uuid, uuid, text) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
20. `revoke all on function public.create_wallet_top_up(uuid, int, text, text) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
21. `revoke all on function public.settle_wallet_top_up(uuid, text, text, text) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
22. `revoke all on function public.adjust_wallet_balance(uuid, int, text, text, text, text, uuid) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
23. `revoke all on function public.reverse_wallet_ledger_entry(uuid, text) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
24. `grant execute on function public.ensure_wallet_account(uuid, uuid, text) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
25. `grant execute on function public.create_wallet_top_up(uuid, int, text, text) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
26. `grant execute on function public.settle_wallet_top_up(uuid, text, text, text) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
27. `grant execute on function public.adjust_wallet_balance(uuid, int, text, text, text, text, uuid) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
28. `grant execute on function public.reverse_wallet_ledger_entry(uuid, text) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
29. `insert into public.role_permissions (role_id, permission_id) select r.id, p.id from public.roles r cross join public.permissions p where r.key = 'treasurer' and p.key = 'wallet.adj…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)

### 20260723172034_payment_provider_webhooks

Classification: **unable to determine safely**. Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.

1. `create table if not exists public.payment_providers ( provider_key text primary key, display_name text not null, provider_type text not null check (provider_type in ('manual','stri…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
2. `create table if not exists public.payment_webhook_events ( id uuid primary key default gen_random_uuid(), provider text not null, provider_event_id text not null, event_type text n…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
3. `create index if not exists payment_providers_active_idx on public.payment_providers (is_active, provider_type);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
4. `create index if not exists payment_webhook_events_received_idx on public.payment_webhook_events (received_at desc);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
5. `create index if not exists payment_webhook_events_payment_idx on public.payment_webhook_events (payment_id, received_at desc) where payment_id is not null;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
6. `create index if not exists payment_webhook_events_provider_payment_idx on public.payment_webhook_events (provider, provider_payment_id) where provider_payment_id is not null;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
7. `alter table public.payment_providers enable row level security;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
8. `alter table public.payment_webhook_events enable row level security;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
9. `drop policy if exists payment_providers_finance_read on public.payment_providers;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
10. `create policy payment_providers_finance_read on public.payment_providers for select to authenticated using (app_private.has_permission('finance.read') or app_private.has_permission…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
11. `drop policy if exists payment_webhook_events_finance_read on public.payment_webhook_events;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
12. `create policy payment_webhook_events_finance_read on public.payment_webhook_events for select to authenticated using (app_private.has_permission('finance.read') or app_private.has_…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
13. `insert into public.payment_providers (provider_key, display_name, provider_type, supports_webhooks, webhook_secret_ref) values ('manual', 'Manual settlement', 'manual', false, null…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
14. `create or replace function public.create_wallet_top_up( target_wallet_id uuid, top_up_amount_cents int, provider text default 'manual', idempotency_key text default null ) returns …` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
15. `create or replace function public.process_payment_webhook( provider text, provider_event_id text, event_type text, provider_payment_ref text default null, target_payment_id uuid de…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
16. `grant select on public.payment_providers to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
17. `grant select on public.payment_webhook_events to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
18. `revoke all on function public.process_payment_webhook(text, text, text, text, uuid, text, jsonb) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
19. `grant execute on function public.process_payment_webhook(text, text, text, text, uuid, text, jsonb) to service_role;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
20. `revoke all on function public.create_wallet_top_up(uuid, int, text, text) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
21. `grant execute on function public.create_wallet_top_up(uuid, int, text, text) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)

### 20260723173101_volunteer_shift_operations

Classification: **unable to determine safely**. Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.

1. `create index if not exists volunteer_assignments_shift_status_idx on public.volunteer_assignments (shift_id, status);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
2. `create index if not exists volunteer_shifts_status_starts_idx on public.volunteer_shifts (status, starts_at);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
3. `create or replace function app_private.refresh_volunteer_shift_status(target_shift_id uuid) returns text language plpgsql security definer set search_path = public, extensions as $…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
4. `create or replace function public.request_volunteer_shift(target_shift_id uuid) returns table ( assignment_id uuid, assignment_status text, shift_status text ) language plpgsql sec…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
5. `create or replace function public.update_volunteer_assignment( target_assignment_id uuid, target_status text, note text default null ) returns table ( assignment_id uuid, assignmen…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
6. `create or replace function public.update_volunteer_shift_status( target_shift_id uuid, target_status text, note text default null ) returns table ( shift_id uuid, shift_status text…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
7. `drop policy if exists volunteer_assignments_manage on public.volunteer_assignments;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
8. `drop policy if exists volunteer_assignments_admin_manage on public.volunteer_assignments;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
9. `create policy volunteer_assignments_admin_manage on public.volunteer_assignments for all to authenticated using (app_private.has_permission('volunteers.manage')) with check (app_pr…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
10. `grant select on public.volunteer_opportunities to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
11. `grant select on public.volunteer_shifts to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
12. `grant select on public.volunteer_assignments to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
13. `revoke all on function public.request_volunteer_shift(uuid) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
14. `revoke all on function public.update_volunteer_assignment(uuid, text, text) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
15. `revoke all on function public.update_volunteer_shift_status(uuid, text, text) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
16. `grant execute on function public.request_volunteer_shift(uuid) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
17. `grant execute on function public.update_volunteer_assignment(uuid, text, text) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
18. `grant execute on function public.update_volunteer_shift_status(uuid, text, text) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)

### 20260723173938_coaching_resource_library

Classification: **unable to determine safely**. Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.

1. `alter table public.coaching_resources add column if not exists slug text, add column if not exists external_url text, add column if not exists attachment_file_id uuid references pu…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
2. `create unique index if not exists coaching_resources_slug_unique on public.coaching_resources (slug) where slug is not null;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
3. `create index if not exists coaching_resources_status_visibility_idx on public.coaching_resources (status, visibility, updated_at desc);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
4. `create index if not exists coaching_resources_resource_type_idx on public.coaching_resources (resource_type, updated_at desc);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
5. `create index if not exists coaching_resources_age_group_gin_idx on public.coaching_resources using gin (age_group_tags);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
6. `create index if not exists coaching_resources_skill_level_gin_idx on public.coaching_resources using gin (skill_level_tags);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
7. `create or replace function app_private.slugify(value text) returns text language sql immutable set search_path = public, extensions as $$ select nullif(trim(both '-' from regexp_re…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
8. `create or replace function app_private.coaching_resource_before_write() returns trigger language plpgsql security definer set search_path = public, extensions as $$ begin new.slug …` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
9. `drop trigger if exists coaching_resource_before_write on public.coaching_resources;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
10. `create trigger coaching_resource_before_write before insert or update on public.coaching_resources for each row execute function app_private.coaching_resource_before_write();` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
11. `grant select on public.coaching_resources to anon, authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
12. `grant insert, update, delete on public.coaching_resources to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
13. `grant select on public.file_records to anon, authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)

### 20260723175036_public_content_publishing

Classification: **unable to determine safely**. Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.

1. `create index if not exists content_articles_public_publish_idx on public.content_articles (workflow_status, publish_at desc, updated_at desc);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
2. `create index if not exists content_articles_category_idx on public.content_articles (category, publish_at desc) where workflow_status = 'published';` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
3. `create index if not exists content_articles_tags_gin_idx on public.content_articles using gin (tags);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
4. `create index if not exists club_announcements_public_idx on public.club_announcements (status, audience, priority, starts_at, ends_at);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
5. `create index if not exists sponsors_public_display_idx on public.sponsors (status, display_priority, name);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
6. `create or replace function app_private.content_article_before_write() returns trigger language plpgsql security definer set search_path = public, extensions as $$ begin new.slug :=…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
7. `drop trigger if exists content_article_before_write on public.content_articles;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
8. `create trigger content_article_before_write before insert or update on public.content_articles for each row execute function app_private.content_article_before_write();` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
9. `grant select on public.content_articles to anon, authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
10. `grant insert, update, delete on public.content_articles to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
11. `grant select on public.club_announcements to anon, authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
12. `grant insert, update, delete on public.club_announcements to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
13. `grant select on public.sponsors to anon, authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
14. `grant insert, update, delete on public.sponsors to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)

### 20260723175848_notification_preferences_outbox_worker

Classification: **unable to determine safely**. Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.

1. `create table if not exists public.notification_preferences ( user_id uuid not null references public.profiles(id) on delete cascade, channel text not null check (channel in ('in_ap…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
2. `create index if not exists notification_preferences_user_idx on public.notification_preferences (user_id, channel, enabled);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
3. `alter table public.notification_preferences enable row level security;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
4. `drop policy if exists notification_preferences_own_read on public.notification_preferences;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
5. `create policy notification_preferences_own_read on public.notification_preferences for select to authenticated using (user_id = auth.uid() or app_private.has_permission('communicat…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
6. `drop policy if exists notification_preferences_own_insert on public.notification_preferences;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
7. `create policy notification_preferences_own_insert on public.notification_preferences for insert to authenticated with check (user_id = auth.uid() or app_private.has_permission('com…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
8. `drop policy if exists notification_preferences_own_update on public.notification_preferences;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
9. `create policy notification_preferences_own_update on public.notification_preferences for update to authenticated using (user_id = auth.uid() or app_private.has_permission('communic…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
10. `grant select, insert, update on public.notification_preferences to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
11. `alter table public.notifications add column if not exists category text not null default 'general', add column if not exists action_url text, add column if not exists expires_at ti…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
12. `create index if not exists notifications_recipient_unread_idx on public.notifications (recipient_id, created_at desc) where read_at is null;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
13. `create index if not exists notifications_related_idx on public.notifications (related_entity_type, related_entity_id) where related_entity_type is not null and related_entity_id is…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
14. `create unique index if not exists notifications_recipient_dedupe_idx on public.notifications (recipient_id, dedupe_key) where dedupe_key is not null;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
15. `grant select, insert, update on public.notifications to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
16. `alter table public.communication_outbox add column if not exists category text not null default 'general', add column if not exists dedupe_key text, add column if not exists priori…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
17. `alter table public.communication_outbox drop constraint if exists communication_outbox_status_check;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
18. `alter table public.communication_outbox add constraint communication_outbox_status_check check (status in ('pending', 'processing', 'sent', 'failed', 'cancelled'));` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
19. `create index if not exists communication_outbox_pending_idx on public.communication_outbox (status, scheduled_for, priority desc, created_at) where status in ('pending', 'failed');…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
20. `create index if not exists communication_outbox_recipient_idx on public.communication_outbox (recipient_id, created_at desc);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
21. `create unique index if not exists communication_outbox_dedupe_idx on public.communication_outbox (dedupe_key) where dedupe_key is not null;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
22. `grant select, insert, update on public.communication_outbox to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
23. `grant select, insert, update on public.communication_outbox to service_role;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
24. `create or replace function app_private.notification_channel_enabled( p_user_id uuid, p_channel text, p_category text default 'general' ) returns boolean language plpgsql security d…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
25. `revoke all on function app_private.notification_channel_enabled(uuid, text, text) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
26. `create or replace function public.enqueue_admin_notification( p_recipient_id uuid, p_title text, p_body text, p_category text default 'general', p_channels text[] default array['in…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
27. `revoke all on function public.enqueue_admin_notification(uuid, text, text, text, text[], text, jsonb, text, uuid, text, text, timestamptz) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
28. `grant execute on function public.enqueue_admin_notification(uuid, text, text, text, text[], text, jsonb, text, uuid, text, text, timestamptz) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
29. `create or replace function public.claim_communication_outbox( p_worker_id text, p_limit int default 25 ) returns setof public.communication_outbox language plpgsql security invoker…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
30. `revoke all on function public.claim_communication_outbox(text, int) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
31. `grant execute on function public.claim_communication_outbox(text, int) to service_role;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
32. `create or replace function public.complete_communication_outbox( p_outbox_id uuid, p_worker_id text, p_external_message_id text default null ) returns boolean language plpgsql secu…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
33. `revoke all on function public.complete_communication_outbox(uuid, text, text) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
34. `grant execute on function public.complete_communication_outbox(uuid, text, text) to service_role;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
35. `create or replace function public.fail_communication_outbox( p_outbox_id uuid, p_worker_id text, p_failure_reason text, p_retry_after_seconds int default 300 ) returns boolean lang…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
36. `revoke all on function public.fail_communication_outbox(uuid, text, text, int) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
37. `grant execute on function public.fail_communication_outbox(uuid, text, text, int) to service_role;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)

### 20260724142332_portal_restructure_completion

Classification: **unable to determine safely**. Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.

1. `insert into public.permissions (key, name, description) values ('team_access.review', 'Review team access requests', 'Approve or reject team access requests for assigned teams.'), …` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
2. `insert into public.role_permissions (role_id, permission_id) select r.id, p.id from public.roles r join public.permissions p on p.key in ('team_access.review', 'team_members.manage…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
3. `insert into public.role_permissions (role_id, permission_id) select r.id, p.id from public.roles r join public.permissions p on p.key in ('team_access.review', 'team_members.manage…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
4. `insert into public.role_permissions (role_id, permission_id) select r.id, p.id from public.roles r join public.permissions p on p.key in ('merchandise.store_access') where r.key = …` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
5. `insert into public.roles (key, name, description, is_system, is_sensitive) values ('canteen_staff', 'Canteen staff', 'Operational access for canteen orders, voucher scanning and wa…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
6. `insert into public.role_permissions (role_id, permission_id) select r.id, p.id from public.roles r join public.permissions p on p.key in ( 'canteen.orders.manage', 'canteen.voucher…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
7. `update public.profiles set communication_sms = false where communication_sms is distinct from false;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
8. `delete from public.notification_preferences where channel = 'sms';` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
9. `alter table public.notification_preferences drop constraint if exists notification_preferences_channel_check;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
10. `alter table public.notification_preferences add constraint notification_preferences_channel_check check (channel in ('in_app', 'email'));` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
11. `create table if not exists public.team_access_requests ( id uuid primary key default gen_random_uuid(), requester_id uuid not null references public.profiles(id) on delete cascade,…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
12. `create unique index if not exists team_access_requests_pending_unique on public.team_access_requests (requester_id, team_id, requested_relationship) where status = 'pending';` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
13. `create index if not exists team_access_requests_team_status_idx on public.team_access_requests (team_id, status, created_at desc);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
14. `drop trigger if exists team_access_requests_set_updated_at on public.team_access_requests;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
15. `create trigger team_access_requests_set_updated_at before update on public.team_access_requests for each row execute function app_private.set_updated_at();` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
16. `alter table public.team_access_requests enable row level security;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
17. `create or replace function app_private.can_review_team_access(target_team_id uuid) returns boolean language sql stable security definer set search_path = public, extensions as $$ s…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
18. `revoke all on function app_private.can_review_team_access(uuid) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
19. `drop policy if exists team_access_requests_own_read on public.team_access_requests;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
20. `create policy team_access_requests_own_read on public.team_access_requests for select to authenticated using (requester_id = auth.uid() or app_private.can_review_team_access(team_i…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
21. `drop policy if exists team_access_requests_own_insert on public.team_access_requests;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
22. `create policy team_access_requests_own_insert on public.team_access_requests for insert to authenticated with check ( requester_id = auth.uid() and status = 'pending' and not app_p…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
23. `drop policy if exists team_access_requests_reviewer_update on public.team_access_requests;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
24. `create policy team_access_requests_reviewer_update on public.team_access_requests for update to authenticated using (requester_id = auth.uid() or app_private.can_review_team_access…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
25. `grant select, insert, update on public.team_access_requests to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
26. `create or replace function public.request_team_access( target_team_id uuid, requested_relationship text default 'parent', request_note text default null ) returns uuid language plp…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
27. `create or replace function public.review_team_access_request( target_request_id uuid, review_status text, internal_note text default null ) returns uuid language plpgsql security d…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
28. `revoke all on function public.request_team_access(uuid, text, text) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
29. `revoke all on function public.review_team_access_request(uuid, text, text) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
30. `grant execute on function public.request_team_access(uuid, text, text) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
31. `grant execute on function public.review_team_access_request(uuid, text, text) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
32. `create table if not exists public.managed_child_accounts ( id uuid primary key default gen_random_uuid(), child_user_id uuid not null unique references public.profiles(id) on delet…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
33. `drop trigger if exists managed_child_accounts_set_updated_at on public.managed_child_accounts;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
34. `create trigger managed_child_accounts_set_updated_at before update on public.managed_child_accounts for each row execute function app_private.set_updated_at();` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
35. `alter table public.managed_child_accounts enable row level security;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
36. `drop policy if exists managed_child_accounts_family_read on public.managed_child_accounts;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
37. `create policy managed_child_accounts_family_read on public.managed_child_accounts for select to authenticated using ( child_user_id = auth.uid() or manager_user_id = auth.uid() or …` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
38. `drop policy if exists managed_child_accounts_manager_update on public.managed_child_accounts;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
39. `create policy managed_child_accounts_manager_update on public.managed_child_accounts for update to authenticated using (manager_user_id = auth.uid() or app_private.has_permission('…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
40. `grant select, insert, update on public.managed_child_accounts to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
41. `grant select, insert, update on public.managed_child_accounts to service_role;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
42. `create table if not exists public.store_access_tokens ( id uuid primary key default gen_random_uuid(), user_id uuid not null references public.profiles(id) on delete cascade, token…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
43. `create index if not exists store_access_tokens_user_idx on public.store_access_tokens (user_id, created_at desc);` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
44. `alter table public.store_access_tokens enable row level security;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
45. `drop policy if exists store_access_tokens_own_read on public.store_access_tokens;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
46. `create policy store_access_tokens_own_read on public.store_access_tokens for select to authenticated using (user_id = auth.uid() or app_private.has_permission('merchandise.manage')…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
47. `grant select on public.store_access_tokens to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
48. `grant select, insert, update on public.store_access_tokens to service_role;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
49. `create table if not exists public.voucher_templates ( id uuid primary key default gen_random_uuid(), name text not null, description text, voucher_type text not null check (voucher…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
50. `drop trigger if exists voucher_templates_set_updated_at on public.voucher_templates;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
51. `create trigger voucher_templates_set_updated_at before update on public.voucher_templates for each row execute function app_private.set_updated_at();` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
52. `create table if not exists public.voucher_campaigns ( id uuid primary key default gen_random_uuid(), template_id uuid not null references public.voucher_templates(id) on delete res…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
53. `alter table public.voucher_templates enable row level security;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
54. `alter table public.voucher_campaigns enable row level security;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
55. `drop policy if exists voucher_templates_read_authorised on public.voucher_templates;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
56. `create policy voucher_templates_read_authorised on public.voucher_templates for select to authenticated using (status = 'active' or app_private.has_permission('wallet.vouchers.mana…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
57. `drop policy if exists voucher_templates_manage_authorised on public.voucher_templates;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
58. `create policy voucher_templates_manage_authorised on public.voucher_templates for all to authenticated using (app_private.has_permission('wallet.vouchers.manage') or app_private.ha…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
59. `drop policy if exists voucher_campaigns_manage_authorised on public.voucher_campaigns;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
60. `create policy voucher_campaigns_manage_authorised on public.voucher_campaigns for all to authenticated using (app_private.has_permission('wallet.vouchers.manage') or app_private.ha…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
61. `grant select, insert, update, delete on public.voucher_templates to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
62. `grant select, insert, update, delete on public.voucher_campaigns to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
63. `alter table public.voucher_issuances add column if not exists template_id uuid references public.voucher_templates(id) on delete set null, add column if not exists campaign_id uuid…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
64. `alter table public.content_articles drop constraint if exists content_articles_workflow_status_check;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
65. `update public.content_articles set workflow_status = case when workflow_status in ('published','scheduled') then 'active' else 'inactive' end where workflow_status in ('draft','in_…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
66. `alter table public.content_articles alter column workflow_status set default 'inactive';` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
67. `alter table public.content_articles add constraint content_articles_workflow_status_check check (workflow_status in ('active','inactive'));` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
68. `alter table public.club_announcements drop constraint if exists club_announcements_status_check;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
69. `update public.club_announcements set status = case when status = 'published' then 'active' else 'inactive' end where status in ('draft','published','archived');` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
70. `alter table public.club_announcements alter column status set default 'inactive';` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
71. `alter table public.club_announcements add constraint club_announcements_status_check check (status in ('active','inactive'));` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
72. `alter table public.club_events drop constraint if exists club_events_status_check;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
73. `update public.club_events set status = case when status = 'published' then 'active' else 'inactive' end where status in ('draft','published','archived');` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
74. `alter table public.club_events alter column status set default 'inactive';` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
75. `alter table public.club_events add constraint club_events_status_check check (status in ('active','inactive','cancelled','completed'));` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
76. `alter table public.coaching_resources drop constraint if exists coaching_resources_status_check;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
77. `update public.coaching_resources set status = case when status = 'published' then 'active' else 'inactive' end where status in ('draft','published','archived');` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
78. `alter table public.coaching_resources alter column status set default 'inactive';` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
79. `alter table public.coaching_resources add constraint coaching_resources_status_check check (status in ('active','inactive'));` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
80. `alter table public.team_posts drop constraint if exists team_posts_status_check;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
81. `update public.team_posts set status = case when status = 'published' then 'active' else 'inactive' end where status in ('draft','published','archived');` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
82. `alter table public.team_posts alter column status set default 'active';` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
83. `alter table public.team_posts add constraint team_posts_status_check check (status in ('active','inactive'));` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
84. `alter table public.merchandise_products drop constraint if exists merchandise_products_status_check;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
85. `update public.merchandise_products set status = case when status = 'active' then 'active' else 'inactive' end where status in ('draft','active','archived');` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
86. `alter table public.merchandise_products alter column status set default 'active';` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
87. `alter table public.merchandise_products add constraint merchandise_products_status_check check (status in ('active','inactive'));` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
88. `drop policy if exists content_public_published on public.content_articles;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
89. `drop policy if exists content_public_active on public.content_articles;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
90. `create policy content_public_active on public.content_articles for select to anon, authenticated using (workflow_status = 'active' and (publish_at is null or publish_at <= now()));…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
91. `drop policy if exists announcements_public_published on public.club_announcements;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
92. `drop policy if exists announcements_public_active on public.club_announcements;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
93. `create policy announcements_public_active on public.club_announcements for select to anon, authenticated using (status = 'active' and (starts_at is null or starts_at <= now()) and …` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
94. `drop policy if exists team_posts_accessible_read on public.team_posts;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
95. `create policy team_posts_accessible_read on public.team_posts for select to authenticated using (status = 'active' and app_private.can_access_team(team_id));` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
96. `drop policy if exists coaching_resources_read on public.coaching_resources;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
97. `drop policy if exists coaching_resources_active_read on public.coaching_resources;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
98. `create policy coaching_resources_active_read on public.coaching_resources for select to authenticated using ( status = 'active' and ( visibility = 'public' or app_private.has_permi…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
99. `create or replace function app_private.notification_channel_enabled( p_user_id uuid, p_channel text, p_category text default 'general' ) returns boolean language plpgsql security d…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
100. `create or replace function public.enqueue_admin_notification( p_recipient_id uuid, p_title text, p_body text, p_category text default 'general', p_channels text[] default array['in…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
101. `revoke all on function public.enqueue_admin_notification(uuid, text, text, text, text[], text, jsonb, text, uuid, text, text, timestamptz) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
102. `grant execute on function public.enqueue_admin_notification(uuid, text, text, text, text[], text, jsonb, text, uuid, text, text, timestamptz) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
103. `alter table public.teams drop constraint if exists teams_status_check;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
104. `update public.teams set status = case when status = 'active' then 'active' else 'inactive' end where status in ('draft','active','archived');` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
105. `alter table public.teams alter column status set default 'active';` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
106. `alter table public.teams add constraint teams_status_check check (status in ('active','inactive'));` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
107. `alter table public.sponsors drop constraint if exists sponsors_status_check;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
108. `update public.sponsors set status = case when status = 'active' then 'active' else 'inactive' end where status in ('active','inactive','archived');` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
109. `alter table public.sponsors alter column status set default 'active';` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
110. `alter table public.sponsors add constraint sponsors_status_check check (status in ('active','inactive'));` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
111. `create table if not exists public.wallet_qr_tokens ( id uuid primary key default gen_random_uuid(), wallet_account_id uuid not null references public.wallet_accounts(id) on delete …` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
112. `alter table public.wallet_qr_tokens enable row level security;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
113. `drop policy if exists wallet_qr_tokens_owner_read on public.wallet_qr_tokens;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
114. `create policy wallet_qr_tokens_owner_read on public.wallet_qr_tokens for select to authenticated using ( exists ( select 1 from public.wallet_accounts wa where wa.id = wallet_accou…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
115. `drop policy if exists wallet_qr_tokens_owner_insert on public.wallet_qr_tokens;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
116. `create policy wallet_qr_tokens_owner_insert on public.wallet_qr_tokens for insert to authenticated with check ( exists ( select 1 from public.wallet_accounts wa where wa.id = walle…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
117. `grant select, insert, update on public.wallet_qr_tokens to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
118. `create or replace function public.process_wallet_qr_purchase( wallet_display_code text, purchase_amount_cents int, purchase_description text default 'Canteen wallet purchase', idem…` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
119. `revoke all on function public.process_wallet_qr_purchase(text, int, text, text) from public;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)
120. `grant execute on function public.process_wallet_qr_purchase(text, int, text, text) to authenticated;` — **unable to determine safely** (Expected tables, functions, policies, triggers and indexes are substantially present, but catalog presence cannot prove every DML, grant, function body and later superseding change.)

### 20260726125756_social_hub

Classification: **fully applied manually**. Complete normalized SQL matches recorded production migration 20260726130513.

1. `insert into public.permissions (key, name, description) values ('social_profiles.view', 'View social profiles', 'View configured official social profiles'), ('social_profiles.manag…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726130513.)
2. `create table public.social_profiles ( id uuid primary key default gen_random_uuid(), platform text not null check (platform in ('instagram','facebook','tiktok')), display_name text…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726130513.)
3. `create table public.social_posts ( id uuid primary key default gen_random_uuid(), platform text not null check (platform in ('instagram','facebook','tiktok')), post_url text not nu…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726130513.)
4. `create trigger social_profiles_set_updated_at before update on public.social_profiles for each row execute function app_private.set_updated_at();` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726130513.)
5. `create trigger social_posts_set_updated_at before update on public.social_posts for each row execute function app_private.set_updated_at();` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726130513.)
6. `create or replace function app_private.audit_social_change() returns trigger language plpgsql security definer set search_path = public, app_private as $$ declare action_name text;…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726130513.)
7. `create trigger social_profiles_audit after insert or update or delete on public.social_profiles for each row execute function app_private.audit_social_change();` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726130513.)
8. `create trigger social_posts_audit after insert or update or delete on public.social_posts for each row execute function app_private.audit_social_change();` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726130513.)
9. `create index social_profiles_public_idx on public.social_profiles (sort_order, platform) where active;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726130513.)
10. `create index social_posts_public_idx on public.social_posts (featured desc, sort_order, published_at desc, created_at desc) where active;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726130513.)
11. `alter table public.social_profiles enable row level security;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726130513.)
12. `alter table public.social_posts enable row level security;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726130513.)
13. `create policy social_profiles_public_read on public.social_profiles for select to anon using (active);` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726130513.)
14. `create policy social_profiles_authorised_read on public.social_profiles for select to authenticated using (active or app_private.has_permission('social_profiles.view') or app_priva…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726130513.)
15. `create policy social_profiles_authorised_manage on public.social_profiles for all to authenticated using (app_private.has_permission('social_profiles.manage')) with check (app_priv…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726130513.)
16. `create policy social_posts_public_read on public.social_posts for select to anon using (active);` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726130513.)
17. `create policy social_posts_authorised_read on public.social_posts for select to authenticated using (active or app_private.has_permission('social_posts.view') or app_private.has_pe…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726130513.)
18. `create policy social_posts_authorised_manage on public.social_posts for all to authenticated using (app_private.has_permission('social_posts.manage')) with check (app_private.has_p…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726130513.)
19. `grant select on public.social_profiles, public.social_posts to anon;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726130513.)
20. `grant select, insert, update, delete on public.social_profiles, public.social_posts to authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726130513.)
21. `grant select, insert, update, delete on public.social_profiles, public.social_posts to service_role;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726130513.)
22. `insert into public.social_profiles (platform, display_name, username, profile_url, sort_order) values ('instagram', 'Greenacre Eagles FC', '@greenacreeagles', 'https://www.instagra…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726130513.)

### 20260726131056_event_ticketing

Classification: **unable to determine safely**. Production has event_ticketing under 20260726131535, but normalized SQL differs.

1. `insert into public.permissions(key,name,description) values ('events.tickets.scan','Scan event tickets','View event ticket details at entry'), ('events.tickets.redeem','Redeem even…` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
2. `alter table public.club_events add column if not exists image_object_key text, add column if not exists per_user_ticket_limit int check(per_user_ticket_limit is null or per_user_ti…` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
3. `create table public.club_event_ticket_types( id uuid primary key default gen_random_uuid(), event_id uuid not null references public.club_events(id) on delete cascade, name text no…` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
4. `create table public.club_event_orders( id uuid primary key default gen_random_uuid(), order_number text not null unique, user_id uuid not null references public.profiles(id) on del…` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
5. `create table public.club_event_order_items( id uuid primary key default gen_random_uuid(), order_id uuid not null references public.club_event_orders(id) on delete cascade, ticket_…` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
6. `alter table public.voucher_issuances drop constraint if exists voucher_issuances_voucher_type_check;` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
7. `alter table public.voucher_issuances add constraint voucher_issuances_voucher_type_check check(voucher_type in('fixed_amount','specific_product','category','meal_deal','declining_b…` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
8. `create table public.club_event_tickets( id uuid primary key default gen_random_uuid(), order_id uuid not null references public.club_event_orders(id) on delete restrict, order_item…` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
9. `create index club_event_ticket_types_public_idx on public.club_event_ticket_types(event_id,sort_order) where active;` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
10. `create index club_event_orders_user_idx on public.club_event_orders(user_id,created_at desc);` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
11. `create index club_event_orders_event_idx on public.club_event_orders(event_id,status,payment_status);` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
12. `create index club_event_tickets_owner_idx on public.club_event_tickets(owner_user_id,event_id,issued_at desc);` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
13. `create index club_event_tickets_event_status_idx on public.club_event_tickets(event_id,ticket_type_id,status);` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
14. `create trigger club_event_ticket_types_updated before update on public.club_event_ticket_types for each row execute function app_private.set_updated_at();` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
15. `create trigger club_event_orders_updated before update on public.club_event_orders for each row execute function app_private.set_updated_at();` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
16. `create trigger club_event_tickets_updated before update on public.club_event_tickets for each row execute function app_private.set_updated_at();` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
17. `alter table public.club_event_ticket_types enable row level security;` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
18. `alter table public.club_event_orders enable row level security;` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
19. `alter table public.club_event_order_items enable row level security;` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
20. `alter table public.club_event_tickets enable row level security;` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
21. `create policy event_ticket_types_read on public.club_event_ticket_types for select to anon,authenticated using(active and exists(select 1 from public.club_events e where e.id=event…` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
22. `create policy event_ticket_types_admin on public.club_event_ticket_types for all to authenticated using(app_private.has_permission('events.manage')) with check(app_private.has_perm…` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
23. `create policy event_orders_owner_read on public.club_event_orders for select to authenticated using(user_id=(select auth.uid()) or app_private.has_permission('events.orders.read') …` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
24. `create policy event_order_items_owner_read on public.club_event_order_items for select to authenticated using(exists(select 1 from public.club_event_orders o where o.id=order_id an…` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
25. `create policy event_tickets_owner_read on public.club_event_tickets for select to authenticated using(owner_user_id=(select auth.uid()) or app_private.has_permission('events.ticket…` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
26. `grant select on public.club_event_ticket_types to anon,authenticated;` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
27. `grant select on public.club_event_orders,public.club_event_order_items,public.club_event_tickets to authenticated;` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
28. `grant all on public.club_event_ticket_types,public.club_event_orders,public.club_event_order_items,public.club_event_tickets to service_role;` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
29. `create or replace function app_private.issue_event_order(target_order uuid) returns int language plpgsql security definer set search_path=public,app_private,extensions as $$ declar…` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
30. `create or replace function public.create_event_ticket_order(ticket_type uuid,ticket_quantity int,request_key text,payment_provider text default 'manual') returns table(order_id uui…` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
31. `revoke all on function public.create_event_ticket_order(uuid,int,text,text) from public;` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
32. `grant execute on function public.create_event_ticket_order(uuid,int,text,text) to authenticated;` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
33. `create or replace function app_private.complete_event_payment() returns trigger language plpgsql security definer set search_path=public,app_private as $$ declare oid uuid; begin i…` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
34. `create trigger payments_complete_event_ticket after update of status on public.payments for each row execute function app_private.complete_event_payment();` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
35. `create or replace function public.redeem_event_ticket(redemption_code text) returns table(ticket_id uuid,result text,event_title text,ticket_type text,redeemed_at timestamptz) lang…` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
36. `revoke all on function public.redeem_event_ticket(text) from public;` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)
37. `grant execute on function public.redeem_event_ticket(text) to authenticated;` — **unable to determine safely** (Production has event_ticketing under 20260726131535, but normalized SQL differs.)

### 20260726131544_seed_existing_event_ticket_types

Classification: **fully applied manually**. Complete normalized SQL matches recorded production migration 20260726131614.

1. `insert into public.club_event_ticket_types(event_id,name,description,price_cents,currency,capacity,max_per_order,sort_order) select id,'General admission','Entry to '||title,price_…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726131614.)

### 20260726131813_fix_event_order_conflict

Classification: **fully applied manually**. Complete normalized SQL matches recorded production migration 20260726131859.

1. `do $$ declare definition text; begin select pg_get_functiondef('public.create_event_ticket_order(uuid,int,text,text)'::regprocedure) into definition; definition := replace(definiti…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726131859.)

### 20260726132047_harden_event_ticket_rpcs

Classification: **fully applied manually**. Complete normalized SQL matches recorded production migration 20260726132109.

1. `revoke execute on function public.create_event_ticket_order(uuid,int,text,text) from anon;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726132109.)
2. `revoke execute on function public.redeem_event_ticket(text) from anon;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726132109.)
3. `grant execute on function public.create_event_ticket_order(uuid,int,text,text) to authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726132109.)
4. `grant execute on function public.redeem_event_ticket(text) to authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726132109.)

### 20260726133034_unified_portal_shop

Classification: **fully applied manually**. Complete normalized SQL matches recorded production migration 20260726133228.

1. `insert into public.permissions(key,name,description) values ('shop.products.view','View shop products','View unified canteen and merchandise products'), ('shop.products.manage','Ma…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
2. `alter table public.canteen_products add column if not exists image_object_key text,add column if not exists available_from timestamptz,add column if not exists available_until time…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
3. `alter table public.merchandise_products add column if not exists image_object_key text,add column if not exists available_from timestamptz,add column if not exists available_until …` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
4. `create table public.shop_cart_items( id uuid primary key default gen_random_uuid(),user_id uuid not null references public.profiles(id) on delete cascade, product_type text not nul…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
5. `create unique index shop_cart_canteen_unique on public.shop_cart_items(user_id,canteen_product_id) where canteen_product_id is not null;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
6. `create unique index shop_cart_merch_unique on public.shop_cart_items(user_id,merchandise_variant_id) where merchandise_variant_id is not null;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
7. `create table public.shop_orders( id uuid primary key default gen_random_uuid(),order_number text not null unique,user_id uuid not null references public.profiles(id) on delete rest…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
8. `create table public.shop_order_items( id uuid primary key default gen_random_uuid(),order_id uuid not null references public.shop_orders(id) on delete cascade, product_type text no…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
9. `create table public.shop_fulfilments( id uuid primary key default gen_random_uuid(),order_id uuid not null references public.shop_orders(id) on delete cascade,user_id uuid not null…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
10. `create index shop_orders_user_idx on public.shop_orders(user_id,created_at desc);` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
11. `create index shop_order_items_order_idx on public.shop_order_items(order_id,product_type,status);` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
12. `create index shop_fulfilments_user_idx on public.shop_fulfilments(user_id,status,created_at desc);` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
13. `create trigger shop_cart_updated before update on public.shop_cart_items for each row execute function app_private.set_updated_at();` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
14. `create trigger shop_orders_updated before update on public.shop_orders for each row execute function app_private.set_updated_at();` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
15. `create trigger shop_fulfilments_updated before update on public.shop_fulfilments for each row execute function app_private.set_updated_at();` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
16. `alter table public.shop_cart_items enable row level security;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
17. `alter table public.shop_orders enable row level security;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
18. `alter table public.shop_order_items enable row level security;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
19. `alter table public.shop_fulfilments enable row level security;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
20. `create policy shop_cart_owner on public.shop_cart_items for all to authenticated using(user_id=(select auth.uid())) with check(user_id=(select auth.uid()));` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
21. `create policy shop_orders_owner_read on public.shop_orders for select to authenticated using(user_id=(select auth.uid()) or app_private.has_permission('shop.orders.view') or app_pr…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
22. `create policy shop_items_owner_read on public.shop_order_items for select to authenticated using(exists(select 1 from public.shop_orders o where o.id=order_id and(o.user_id=(select…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
23. `create policy shop_fulfilments_owner_read on public.shop_fulfilments for select to authenticated using(user_id=(select auth.uid()) or app_private.has_permission('shop.canteen.scan'…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
24. `grant select,insert,update,delete on public.shop_cart_items to authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
25. `grant select on public.shop_orders,public.shop_order_items,public.shop_fulfilments to authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
26. `grant all on public.shop_cart_items,public.shop_orders,public.shop_order_items,public.shop_fulfilments to service_role;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
27. `create or replace function app_private.confirm_shop_order(target uuid) returns int language plpgsql security definer set search_path=public,app_private,extensions as $$ declare o p…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
28. `create or replace function public.checkout_shop_cart(request_key text,payment_provider text default 'manual') returns table(order_id uuid,order_status text,payment_status text,tota…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
29. `revoke all on function public.checkout_shop_cart(text,text) from public,anon;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
30. `grant execute on function public.checkout_shop_cart(text,text) to authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
31. `create or replace function app_private.complete_shop_payment() returns trigger language plpgsql security definer set search_path=public,app_private as $$ declare oid uuid;begin if …` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
32. `create trigger payments_complete_shop_order after update of status on public.payments for each row execute function app_private.complete_shop_payment();` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
33. `create or replace function public.redeem_shop_collection(collection_code text) returns table(fulfilment_id uuid,result text,order_number text,collected_at timestamptz) language plp…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
34. `revoke all on function public.redeem_shop_collection(text) from public,anon;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)
35. `grant execute on function public.redeem_shop_collection(text) to authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133228.)

### 20260726133724_harden_unified_shop_checkout

Classification: **fully applied manually**. Complete normalized SQL matches recorded production migration 20260726133822.

1. `alter table public.shop_orders add column if not exists stock_released_at timestamptz;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133822.)
2. `alter function public.checkout_shop_cart(text, text) rename to checkout_shop_cart_impl;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133822.)
3. `revoke all on function public.checkout_shop_cart_impl(text, text) from public, anon, authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133822.)
4. `create function public.checkout_shop_cart( request_key text, payment_provider text default 'manual' ) returns table( order_id uuid, order_status text, payment_status text, total_ce…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133822.)
5. `revoke all on function public.checkout_shop_cart(text, text) from public, anon;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133822.)
6. `grant execute on function public.checkout_shop_cart(text, text) to authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133822.)
7. `create or replace function app_private.release_failed_shop_inventory() returns trigger language plpgsql security definer set search_path = public, app_private as $$ declare target_…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133822.)
8. `create trigger payments_release_failed_shop_inventory after update of status on public.payments for each row execute function app_private.release_failed_shop_inventory();` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726133822.)

### 20260726135312_secure_sponsor_management

Classification: **unable to determine safely**. Production has secure_sponsor_management under 20260726135629, but normalized SQL differs.

1. `insert into public.permissions(key, name, description) values ('sponsors.view', 'View sponsors', 'View sponsor records in the administration portal.') on conflict (key) do update s…` — **unable to determine safely** (Production has secure_sponsor_management under 20260726135629, but normalized SQL differs.)
2. `alter table public.sponsors add column if not exists logo_object_key text, add column if not exists created_by uuid references public.profiles(id) on delete set null, add column if…` — **unable to determine safely** (Production has secure_sponsor_management under 20260726135629, but normalized SQL differs.)
3. `alter table public.sponsors drop constraint if exists sponsors_website_https_check;` — **unable to determine safely** (Production has secure_sponsor_management under 20260726135629, but normalized SQL differs.)
4. `alter table public.sponsors add constraint sponsors_website_https_check check (website_url is null or website_url ~ '^https://[^[:space:]]+$');` — **unable to determine safely** (Production has secure_sponsor_management under 20260726135629, but normalized SQL differs.)
5. `alter table public.sponsors enable row level security;` — **unable to determine safely** (Production has secure_sponsor_management under 20260726135629, but normalized SQL differs.)
6. `drop policy if exists sponsors_public_active on public.sponsors;` — **unable to determine safely** (Production has secure_sponsor_management under 20260726135629, but normalized SQL differs.)
7. `drop policy if exists sponsors_admin_read on public.sponsors;` — **unable to determine safely** (Production has secure_sponsor_management under 20260726135629, but normalized SQL differs.)
8. `drop policy if exists sponsors_manage on public.sponsors;` — **unable to determine safely** (Production has secure_sponsor_management under 20260726135629, but normalized SQL differs.)
9. `create policy sponsors_authenticated_active on public.sponsors for select to authenticated using ( status = 'active' or app_private.has_permission('sponsors.view') or app_private.h…` — **unable to determine safely** (Production has secure_sponsor_management under 20260726135629, but normalized SQL differs.)
10. `create policy sponsors_manage on public.sponsors for all to authenticated using (app_private.has_permission('sponsors.manage')) with check (app_private.has_permission('sponsors.man…` — **unable to determine safely** (Production has secure_sponsor_management under 20260726135629, but normalized SQL differs.)
11. `revoke all on public.sponsors from anon;` — **unable to determine safely** (Production has secure_sponsor_management under 20260726135629, but normalized SQL differs.)
12. `grant select on public.sponsors to authenticated;` — **unable to determine safely** (Production has secure_sponsor_management under 20260726135629, but normalized SQL differs.)
13. `grant insert, update, delete on public.sponsors to authenticated;` — **unable to determine safely** (Production has secure_sponsor_management under 20260726135629, but normalized SQL differs.)
14. `grant all on public.sponsors to service_role;` — **unable to determine safely** (Production has secure_sponsor_management under 20260726135629, but normalized SQL differs.)

### 20260726135934_clean_sponsor_policies

Classification: **fully applied manually**. Complete normalized SQL matches recorded production migration 20260726135955.

1. `drop policy if exists sponsors_manage_insert on public.sponsors;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726135955.)
2. `drop policy if exists sponsors_manage_update on public.sponsors;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726135955.)
3. `drop policy if exists sponsors_manage_delete on public.sponsors;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726135955.)
4. `drop policy if exists sponsors_manage on public.sponsors;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726135955.)
5. `create policy sponsors_manage_insert on public.sponsors for insert to authenticated with check (app_private.has_permission('sponsors.manage'));` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726135955.)
6. `create policy sponsors_manage_update on public.sponsors for update to authenticated using (app_private.has_permission('sponsors.manage')) with check (app_private.has_permission('sp…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726135955.)
7. `create policy sponsors_manage_delete on public.sponsors for delete to authenticated using (app_private.has_permission('sponsors.manage'));` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726135955.)
8. `create index if not exists sponsors_created_by_idx on public.sponsors(created_by);` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726135955.)
9. `create index if not exists sponsors_updated_by_idx on public.sponsors(updated_by);` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726135955.)

### 20260726141508_disable_unified_shop_flow

Classification: **fully applied manually**. Complete normalized SQL matches recorded production migration 20260726141535.

1. `revoke execute on function public.checkout_shop_cart(text, text) from authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726141535.)
2. `revoke execute on function public.redeem_shop_collection(text) from authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726141535.)
3. `revoke select, insert, update, delete on public.shop_cart_items from authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726141535.)

### 20260726144455_repair_portal_content_and_wallets

Classification: **fully applied manually**. Complete normalized SQL matches recorded production migration 20260726144708.

1. `create or replace function app_private.provision_profile_wallet() returns trigger language plpgsql security definer set search_path = public, app_private as $$ begin insert into pu…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726144708.)
2. `drop trigger if exists profiles_provision_wallet on public.profiles;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726144708.)
3. `create trigger profiles_provision_wallet after insert on public.profiles for each row execute function app_private.provision_profile_wallet();` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726144708.)
4. `insert into public.wallet_accounts(owner_id, account_type, status) select p.id, 'user', 'active' from public.profiles p where not exists ( select 1 from public.wallet_accounts w wh…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726144708.)
5. `drop policy if exists announcements_public_active on public.club_announcements;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726144708.)
6. `create policy announcements_public_active on public.club_announcements for select to anon, authenticated using (status = 'active');` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726144708.)
7. `drop policy if exists coaching_public_read on public.coaching_resources;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726144708.)
8. `drop policy if exists coaching_staff_read on public.coaching_resources;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726144708.)
9. `drop policy if exists coaching_resources_active_read on public.coaching_resources;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726144708.)
10. `create policy coaching_public_read on public.coaching_resources for select to anon using (status = 'active' and visibility = 'public');` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726144708.)
11. `create policy coaching_resources_active_read on public.coaching_resources for select to authenticated using ( status = 'active' and ( visibility = 'public' or app_private.has_permi…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260726144708.)

### 20260727062245_member_merchandise_store

Classification: **fully applied manually**. Complete normalized SQL matches recorded production migration 20260727063619.

1. `alter table public.merchandise_orders add column if not exists payment_method text, add column if not exists payment_status text, add column if not exists idempotency_key text;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
2. `update public.merchandise_orders set payment_method = coalesce(payment_method, 'pay_at_club'), payment_status = coalesce( payment_status, case when status = 'awaiting_payment' then…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
3. `alter table public.merchandise_orders alter column payment_method set default 'pay_at_club', alter column payment_method set not null, alter column payment_status set default 'awai…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
4. `alter table public.merchandise_orders drop constraint if exists merchandise_orders_payment_method_check;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
5. `alter table public.merchandise_orders add constraint merchandise_orders_payment_method_check check (payment_method in ('pay_at_club', 'online'));` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
6. `alter table public.merchandise_orders drop constraint if exists merchandise_orders_payment_status_check;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
7. `alter table public.merchandise_orders add constraint merchandise_orders_payment_status_check check (payment_status in ('awaiting_payment', 'paid', 'cancelled', 'refunded', 'partial…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
8. `create unique index if not exists merchandise_orders_customer_idempotency_idx on public.merchandise_orders(customer_id, idempotency_key) where idempotency_key is not null;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
9. `drop policy if exists merchandise_order_history_manager_read on public.merchandise_order_status_history;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
10. `drop policy if exists merchandise_order_history_own_or_manager_read on public.merchandise_order_status_history;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
11. `create policy merchandise_order_history_own_or_manager_read on public.merchandise_order_status_history for select to authenticated using ( app_private.has_permission('merchandise.m…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
12. `create or replace function public.get_merchandise_cart() returns table ( cart_item_id uuid, variant_id uuid, product_id uuid, product_name text, product_description text, category …` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
13. `create or replace function public.set_merchandise_cart_item( target_variant_id uuid, target_quantity int, add_to_existing boolean default false ) returns table ( item_count int, to…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
14. `create or replace function public.clear_merchandise_cart() returns int language plpgsql security definer set search_path = pg_catalog, public, app_private as $$ declare removed_cou…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
15. `create or replace function public.checkout_merchandise_cart( request_key text, target_notes text default null ) returns table ( order_id uuid, order_number text, order_status text,…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
16. `create or replace function app_private.sync_merchandise_payment_state() returns trigger language plpgsql set search_path = pg_catalog, public as $$ begin if new.status = 'paid' and…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
17. `drop trigger if exists merchandise_orders_sync_payment_state on public.merchandise_orders;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
18. `create trigger merchandise_orders_sync_payment_state before update of status on public.merchandise_orders for each row execute function app_private.sync_merchandise_payment_state()…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
19. `revoke insert, update, delete on public.merchandise_orders from anon, authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
20. `revoke insert, update, delete on public.merchandise_order_items from anon, authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
21. `revoke insert, update, delete on public.merchandise_order_status_history from anon, authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
22. `revoke select on public.merchandise_orders, public.merchandise_order_items, public.merchandise_order_status_history, public.shop_cart_items from anon;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
23. `revoke select, insert, update, delete on public.shop_cart_items from authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
24. `grant select on public.merchandise_orders, public.merchandise_order_items, public.merchandise_order_status_history to authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
25. `revoke all on function public.get_merchandise_cart() from public, anon;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
26. `revoke all on function public.set_merchandise_cart_item(uuid, int, boolean) from public, anon;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
27. `revoke all on function public.clear_merchandise_cart() from public, anon;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
28. `revoke all on function public.checkout_merchandise_cart(text, text) from public, anon;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
29. `grant execute on function public.get_merchandise_cart() to authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
30. `grant execute on function public.set_merchandise_cart_item(uuid, int, boolean) to authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
31. `grant execute on function public.clear_merchandise_cart() to authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
32. `grant execute on function public.checkout_merchandise_cart(text, text) to authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)
33. `revoke execute on function public.create_merchandise_order(uuid, int, text, text) from authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727063619.)

### 20260727071413_member_canteen_store

Classification: **fully applied manually**. Complete normalized SQL matches recorded production migration 20260727075752.

1. `alter table public.canteen_orders add column if not exists payment_method text not null default 'pay_at_club', add column if not exists wallet_credit_cents int not null default 0 c…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
2. `update public.canteen_orders set amount_due_cents = case when payment_status = 'paid' then 0 else total_cents end where amount_due_cents = 0 and total_cents > 0;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
3. `alter table public.canteen_orders drop constraint if exists canteen_orders_payment_method_check;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
4. `alter table public.canteen_orders add constraint canteen_orders_payment_method_check check (payment_method in ('pay_at_club', 'wallet_and_voucher'));` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
5. `create unique index if not exists canteen_orders_customer_idempotency_idx on public.canteen_orders(customer_id, idempotency_key) where idempotency_key is not null;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
6. `create table if not exists public.canteen_cart_items ( id uuid primary key default gen_random_uuid(), user_id uuid not null references public.profiles(id) on delete cascade, produc…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
7. `alter table public.canteen_cart_items enable row level security;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
8. `create policy canteen_cart_items_owner_read on public.canteen_cart_items for select to authenticated using ((select auth.uid()) = user_id);` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
9. `create policy canteen_cart_items_owner_insert on public.canteen_cart_items for insert to authenticated with check ((select auth.uid()) = user_id);` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
10. `create policy canteen_cart_items_owner_update on public.canteen_cart_items for update to authenticated using ((select auth.uid()) = user_id) with check ((select auth.uid()) = user_…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
11. `create policy canteen_cart_items_owner_delete on public.canteen_cart_items for delete to authenticated using ((select auth.uid()) = user_id);` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
12. `drop policy if exists canteen_products_public on public.canteen_products;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
13. `create policy canteen_products_public on public.canteen_products for select to anon, authenticated using (is_active);` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
14. `create or replace function public.get_canteen_cart() returns table ( cart_item_id uuid, product_id uuid, product_name text, product_description text, category_name text, image_url …` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
15. `create or replace function public.set_canteen_cart_item( target_product_id uuid, target_quantity int, add_to_existing boolean default false ) returns int language plpgsql security …` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
16. `create or replace function public.clear_canteen_cart() returns int language plpgsql security definer set search_path = '' as $$ declare affected int; begin if auth.uid() is null th…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
17. `create or replace function public.get_canteen_checkout_wallets() returns table(wallet_id uuid, wallet_label text, balance_cents int) language sql security definer set search_path =…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
18. `create or replace function public.get_canteen_checkout_vouchers() returns table(voucher_id uuid, voucher_name text, allowed_product_ids uuid[], expires_at timestamptz) language sql…` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
19. `create or replace function public.checkout_canteen_cart( request_key text, target_wallet_id uuid default null, target_wallet_cents int default 0, target_voucher_ids uuid[] default …` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
20. `grant select, insert, update, delete on public.canteen_cart_items to authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
21. `revoke all on function public.get_canteen_cart() from public, anon;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
22. `revoke all on function public.set_canteen_cart_item(uuid, int, boolean) from public, anon;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
23. `revoke all on function public.clear_canteen_cart() from public, anon;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
24. `revoke all on function public.get_canteen_checkout_wallets() from public, anon;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
25. `revoke all on function public.get_canteen_checkout_vouchers() from public, anon;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
26. `revoke all on function public.checkout_canteen_cart(text, uuid, int, uuid[], uuid, text) from public, anon;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
27. `grant execute on function public.get_canteen_cart() to authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
28. `grant execute on function public.set_canteen_cart_item(uuid, int, boolean) to authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
29. `grant execute on function public.clear_canteen_cart() to authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
30. `grant execute on function public.get_canteen_checkout_wallets() to authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
31. `grant execute on function public.get_canteen_checkout_vouchers() to authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)
32. `grant execute on function public.checkout_canteen_cart(text, uuid, int, uuid[], uuid, text) to authenticated;` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075752.)

### 20260727075829_fix_canteen_checkout_ambiguity

Classification: **fully applied manually**. Complete normalized SQL matches recorded production migration 20260727075901.

1. `create or replace function public.checkout_canteen_cart( request_key text, target_wallet_id uuid default null, target_wallet_cents int default 0, target_voucher_ids uuid[] default …` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727075901.)

### 20260727075959_index_canteen_cart_products

Classification: **fully applied manually**. Complete normalized SQL matches recorded production migration 20260727080031.

1. `create index if not exists canteen_cart_items_product_id_idx on public.canteen_cart_items(product_id);` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727080031.)

### 20260727081500_fix_canteen_payment_method

Classification: **fully applied manually**. Complete normalized SQL matches recorded production migration 20260727080453.

1. `create or replace function public.checkout_canteen_cart( request_key text, target_wallet_id uuid default null, target_wallet_cents int default 0, target_voucher_ids uuid[] default …` — **fully applied manually** (Complete normalized SQL matches recorded production migration 20260727080453.)

### 20260727085028_consolidate_roles_permissions_and_compliance

Classification: **partially applied manually**. Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.

1. `alter table public.roles add column if not exists is_active boolean not null default true;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
2. `alter table public.roles add column if not exists role_kind text not null default 'global';` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
3. `alter table public.roles drop constraint if exists roles_role_kind_check;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
4. `alter table public.roles add constraint roles_role_kind_check check (role_kind in ('global','technical','deprecated'));` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
5. `alter table public.permissions add column if not exists is_active boolean not null default true;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
6. `alter table public.team_staff add column if not exists assigned_by uuid references public.profiles(id) on delete set null;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
7. `alter table public.team_players add column if not exists assigned_by uuid references public.profiles(id) on delete set null;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
8. `update public.roles set key='club_admin',name='Club Admin',description='Full access to all club administration.',is_active=true,role_kind='global',may_request=false,requires_team_s…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
9. `insert into public.roles(key,name,description,is_system,is_sensitive,may_request,requires_team_scope,requires_season_scope,requires_super_admin_approval,sort_order,is_active,role_k…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
10. `do $$ declare old_id uuid; new_id uuid; begin select id into old_id from public.roles where key='canteen_worker'; select id into new_id from public.roles where key='canteen_staff';…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
11. `do $$ declare old_id uuid; new_id uuid; begin select id into old_id from public.roles where key='volunteer_coordinator'; select id into new_id from public.roles where key='registra…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
12. `insert into public.team_staff(team_id,user_id,staff_role,starts_on,ends_on,status,assigned_by,created_at,updated_at) select ura.team_id,ura.user_id,case when r.key in ('coach','ass…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
13. `insert into public.player_records(user_id,season_id,registration_status,created_at,updated_at) select distinct ura.user_id,coalesce(ura.season_id,t.season_id),'registered',ura.crea…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
14. `insert into public.team_players(team_id,player_id,starts_on,ends_on,status,assigned_by,created_at,updated_at) select ura.team_id,pr.id,ura.starts_at::date,ura.ends_at::date,case wh…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
15. `update public.user_role_assignments ura set status='revoked',revoked_at=coalesce(revoked_at,now()),reason=coalesce(reason,'Migrated to a team-scoped position'),updated_at=now() fro…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
16. `update public.team_staff o set status='inactive',updated_at=now() where o.staff_role='assistant_coach' and exists(select 1 from public.team_staff c where c.team_id=o.team_id and c.…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
17. `update public.team_staff set staff_role='coach',updated_at=now() where staff_role='assistant_coach' and status='active';` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
18. `update public.team_staff set status='inactive',updated_at=now() where staff_role='trainer' and status='active';` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
19. `update public.roles set is_active=false,role_kind='deprecated',may_request=false where key not in ('general_user','club_admin','registrar','content_editor','event_manager','merchan…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
20. `update public.user_role_assignments ura set status='revoked',revoked_at=coalesce(revoked_at,now()),reason=coalesce(reason,'Obsolete global role retired during consolidation'),updat…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
21. `insert into public.permissions(key,name,description,is_active) values ('roles.manage','Manage roles','Assign and revoke supported global roles.',true),('registrations.view','View r…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
22. `update public.permissions set is_active=false where key in ('roles.review','team_access.review');` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
23. `delete from public.role_permissions where role_id in(select id from public.roles where key in ('general_user','club_admin','registrar','content_editor','event_manager','merchandise…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
24. `insert into public.role_permissions(role_id,permission_id) select r.id,p.id from public.roles r cross join public.permissions p where r.key='club_admin' and p.key<>'*' and p.is_act…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
25. `insert into public.role_permissions(role_id,permission_id) select r.id,p.id from public.roles r join public.permissions p on p.key='*' where r.key='super_administrator' on conflict…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
26. `with matrix(role_key,permission_key) as (values ('general_user','merchandise.store_access'), ('registrar','users.read'),('registrar','players.manage'),('registrar','families.manage…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
27. `with ranked as (select id,row_number() over(partition by user_id,role_id order by created_at,id) rn from public.user_role_assignments where status='active' and revoked_at is null a…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
28. `create unique index if not exists user_role_assignments_one_active_global on public.user_role_assignments(user_id,role_id) where status='active' and revoked_at is null and team_id …` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
29. `create index if not exists team_staff_user_active_idx on public.team_staff(user_id,team_id) where status='active';` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
30. `create index if not exists team_players_player_active_idx on public.team_players(player_id,team_id) where status='active';` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
31. `create table if not exists public.member_compliance( user_id uuid primary key references public.profiles(id) on delete cascade, volunteer_status text not null default 'pending' che…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
32. `alter table public.member_compliance enable row level security;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
33. `create index if not exists member_compliance_volunteer_status_idx on public.member_compliance(volunteer_status);` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
34. `create index if not exists member_compliance_wwcc_status_expiry_idx on public.member_compliance(wwcc_status,wwcc_expiry_date);` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
35. `insert into public.member_compliance(user_id) select id from public.profiles on conflict(user_id) do nothing;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
36. `drop trigger if exists member_compliance_set_updated_at on public.member_compliance;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
37. `create trigger member_compliance_set_updated_at before update on public.member_compliance for each row execute function app_private.set_updated_at();` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
38. `revoke all on public.member_compliance from anon,authenticated;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
39. `grant select on public.member_compliance to authenticated;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
40. `drop policy if exists member_compliance_read on public.member_compliance;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
41. `create policy member_compliance_read on public.member_compliance for select to authenticated using(user_id=(select auth.uid()) or app_private.has_permission('volunteers.view') or a…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
42. `create or replace function app_private.has_permission(permission_key text,target_team_id uuid default null,target_season_id uuid default null) returns boolean language sql stable s…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
43. `revoke all on function app_private.has_permission(text,uuid,uuid) from public,anon;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
44. `grant execute on function app_private.has_permission(text,uuid,uuid) to authenticated,service_role;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
45. `create or replace function public.has_any_permission(required_keys text[],target_team_id uuid default null,target_season_id uuid default null) returns boolean language sql stable s…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
46. `revoke all on function public.has_any_permission(text[],uuid,uuid) from public,anon;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
47. `grant execute on function public.has_any_permission(text[],uuid,uuid) to authenticated,service_role;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
48. `create or replace function app_private.can_assign_role(target_role_id uuid) returns boolean language sql stable security definer set search_path='' as $$ select case when r.key='su…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
49. `revoke all on function app_private.can_assign_role(uuid) from public,anon,authenticated;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
50. `grant execute on function app_private.can_assign_role(uuid) to service_role;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
51. `create or replace function app_private.can_access_team(target_team_id uuid) returns boolean language sql stable security definer set search_path='' as $$ select app_private.has_per…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
52. `revoke all on function app_private.can_access_team(uuid) from public,anon;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
53. `grant execute on function app_private.can_access_team(uuid) to authenticated,service_role;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
54. `create or replace function app_private.can_manage_team_operations(target_team_id uuid) returns boolean language sql stable security definer set search_path='' as $$ select app_priv…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
55. `revoke all on function app_private.can_manage_team_operations(uuid) from public,anon;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
56. `grant execute on function app_private.can_manage_team_operations(uuid) to authenticated,service_role;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
57. `insert into public.user_role_assignments(user_id,role_id,status,reason) select p.id,r.id,'active','Automatic general-user provisioning' from public.profiles p cross join public.rol…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
58. `create or replace function app_private.handle_new_user() returns trigger language plpgsql security definer set search_path='' as $$ begin insert into public.profiles(id,full_name,e…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
59. `create or replace function public.assign_user_role(target_user_id uuid,target_role_id uuid,target_team_id uuid default null,target_season_id uuid default null,starts_at timestamptz…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
60. `create or replace function public.revoke_user_role(target_assignment_id uuid,revocation_reason text) returns void language plpgsql security definer set search_path='' as $$ declare…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
61. `create or replace function public.update_member_compliance(target_user_id uuid,target_volunteer_status text,target_volunteer_reason text,target_volunteer_notes text,target_wwcc_num…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
62. `revoke all on function public.update_member_compliance(uuid,text,text,text,text,text,date,text,text) from public,anon;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
63. `grant execute on function public.update_member_compliance(uuid,text,text,text,text,text,date,text,text) to authenticated,service_role;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
64. `create or replace function public.save_team_assignment(target_user_id uuid,target_team_id uuid,target_position text,target_status text default 'active',target_starts_on date defaul…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
65. `revoke all on function public.save_team_assignment(uuid,uuid,text,text,date,date) from public,anon;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
66. `grant execute on function public.save_team_assignment(uuid,uuid,text,text,date,date) to authenticated,service_role;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
67. `drop policy if exists team_manage_admin_staff_insert on public.team_staff;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
68. `drop policy if exists team_manage_admin_staff_update on public.team_staff;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
69. `drop policy if exists team_manage_admin_staff_delete on public.team_staff;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
70. `create policy team_manage_admin_staff_insert on public.team_staff for insert to authenticated with check(app_private.has_permission('team_memberships.manage') or app_private.has_pe…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
71. `create policy team_manage_admin_staff_update on public.team_staff for update to authenticated using(app_private.has_permission('team_memberships.manage') or app_private.has_permiss…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
72. `create policy team_manage_admin_staff_delete on public.team_staff for delete to authenticated using(app_private.has_permission('team_memberships.manage') or app_private.has_permiss…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
73. `drop policy if exists team_players_manage_admin_insert on public.team_players;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
74. `drop policy if exists team_players_manage_admin_update on public.team_players;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
75. `drop policy if exists team_players_manage_admin_delete on public.team_players;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
76. `create policy team_players_manage_admin_insert on public.team_players for insert to authenticated with check(app_private.has_permission('team_memberships.manage') or app_private.ha…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
77. `create policy team_players_manage_admin_update on public.team_players for update to authenticated using(app_private.has_permission('team_memberships.manage') or app_private.has_per…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
78. `create policy team_players_manage_admin_delete on public.team_players for delete to authenticated using(app_private.has_permission('team_memberships.manage') or app_private.has_per…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
79. `drop view if exists public.role_catalog;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
80. `create view public.role_catalog with(security_invoker=true) as select r.id,r.key,r.name,r.description,r.is_system,r.is_sensitive,r.may_request,r.requires_team_scope,r.requires_seas…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
81. `grant select on public.role_catalog to authenticated,service_role;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
82. `create or replace function public.admin_dashboard_summary() returns jsonb language sql stable security definer set search_path=pg_catalog,public,app_private,extensions as $$ select…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
83. `revoke all on function public.admin_dashboard_summary() from public,anon;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
84. `grant execute on function public.admin_dashboard_summary() to authenticated,service_role;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
85. `create or replace function public.get_portal_context() returns jsonb language sql stable security definer set search_path = pg_catalog, public, app_private, extensions as $$ with c…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
86. `create or replace function app_private.update_canteen_order_state( target_order_id uuid, target_order_status text default null, target_payment_status text default null, change_reas…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
87. `drop policy if exists orders_read_own_or_staff on public.canteen_orders;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
88. `create policy orders_read_own_or_staff on public.canteen_orders for select to authenticated using(customer_id=auth.uid() or recipient_id=auth.uid() or app_private.has_permission('c…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
89. `drop policy if exists orders_items_read_own_or_staff on public.canteen_order_items;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
90. `create policy orders_items_read_own_or_staff on public.canteen_order_items for select to authenticated using(exists(select 1 from public.canteen_orders o where o.id=order_id and(o.…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
91. `drop policy if exists order_history_staff_read on public.order_status_history;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
92. `create policy order_history_staff_read on public.order_status_history for select to authenticated using(app_private.has_permission('canteen.orders.view') or app_private.has_permiss…` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
93. `revoke insert,update,delete on public.user_role_assignments from authenticated;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
94. `revoke all on function public.assign_user_role(uuid,uuid,uuid,uuid,timestamptz,timestamptz,text) from public,anon;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
95. `grant execute on function public.assign_user_role(uuid,uuid,uuid,uuid,timestamptz,timestamptz,text) to authenticated,service_role;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
96. `revoke all on function public.revoke_user_role(uuid,text) from public,anon;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
97. `grant execute on function public.revoke_user_role(uuid,text) to authenticated,service_role;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
98. `revoke execute on function public.request_role(uuid,uuid,uuid,text,text,text) from public,anon,authenticated;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
99. `revoke execute on function public.withdraw_role_request(uuid,text) from public,anon,authenticated;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
100. `revoke execute on function public.review_role_request(uuid,text,text,timestamptz,timestamptz) from public,anon,authenticated;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
101. `revoke execute on function public.request_team_access(uuid,text,text) from public,anon,authenticated;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
102. `revoke execute on function public.review_team_access_request(uuid,text,text) from public,anon,authenticated;` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)
103. `` — **partially applied manually** (Role/permission structures exist, but member_compliance and its dependent compliance statements are absent.)

### 20260727090000_simplify_canteen_operations

Classification: **unable to determine safely**. Production has simplify_canteen_operations under 20260726151031, but normalized SQL differs.

1. `alter table public.canteen_orders add column if not exists completed_at timestamptz, add column if not exists completed_by uuid references public.profiles(id) on delete set null, a…` — **unable to determine safely** (Production has simplify_canteen_operations under 20260726151031, but normalized SQL differs.)
2. `create or replace function public.complete_canteen_order( target_order_id uuid, completion_source text default 'manual' ) returns table(order_id uuid, order_number text, completed_…` — **unable to determine safely** (Production has simplify_canteen_operations under 20260726151031, but normalized SQL differs.)
3. `create or replace function public.save_canteen_category( target_category_id uuid, category_name text, target_position integer, category_active boolean default true ) returns uuid l…` — **unable to determine safely** (Production has simplify_canteen_operations under 20260726151031, but normalized SQL differs.)
4. `create or replace function public.complete_canteen_order_by_code( order_code text ) returns table(order_id uuid, order_number text, completed_at timestamptz) language plpgsql secur…` — **unable to determine safely** (Production has simplify_canteen_operations under 20260726151031, but normalized SQL differs.)
5. `create or replace function public.issue_canteen_benefits( member_ids uuid[], team_ids uuid[], benefit_type text, amount_cents integer, product_id uuid, expires_at timestamptz, issu…` — **unable to determine safely** (Production has simplify_canteen_operations under 20260726151031, but normalized SQL differs.)
6. `revoke all on function public.complete_canteen_order(uuid, text) from public, anon;` — **unable to determine safely** (Production has simplify_canteen_operations under 20260726151031, but normalized SQL differs.)
7. `revoke all on function public.save_canteen_category(uuid, text, integer, boolean) from public, anon;` — **unable to determine safely** (Production has simplify_canteen_operations under 20260726151031, but normalized SQL differs.)
8. `revoke all on function public.complete_canteen_order_by_code(text) from public, anon;` — **unable to determine safely** (Production has simplify_canteen_operations under 20260726151031, but normalized SQL differs.)
9. `revoke all on function public.issue_canteen_benefits(uuid[], uuid[], text, integer, uuid, timestamptz, text, text) from public, anon;` — **unable to determine safely** (Production has simplify_canteen_operations under 20260726151031, but normalized SQL differs.)
10. `grant execute on function public.complete_canteen_order(uuid, text) to authenticated;` — **unable to determine safely** (Production has simplify_canteen_operations under 20260726151031, but normalized SQL differs.)
11. `grant execute on function public.save_canteen_category(uuid, text, integer, boolean) to authenticated;` — **unable to determine safely** (Production has simplify_canteen_operations under 20260726151031, but normalized SQL differs.)
12. `grant execute on function public.complete_canteen_order_by_code(text) to authenticated;` — **unable to determine safely** (Production has simplify_canteen_operations under 20260726151031, but normalized SQL differs.)
13. `grant execute on function public.issue_canteen_benefits(uuid[], uuid[], text, integer, uuid, timestamptz, text, text) to authenticated;` — **unable to determine safely** (Production has simplify_canteen_operations under 20260726151031, but normalized SQL differs.)
14. `create index if not exists canteen_orders_active_operations_idx on public.canteen_orders(created_at desc) where order_status not in ('draft', 'collected', 'cancelled', 'refunded');…` — **unable to determine safely** (Production has simplify_canteen_operations under 20260726151031, but normalized SQL differs.)

### 20260727110450_simplify_locations_without_venue_tables

Classification: **partially applied manually**. Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.

1. `alter table public.teams add column if not exists home_venue text, add column if not exists training_venue text;` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
2. `alter table public.club_events add column if not exists venue text;` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
3. `alter table public.fixtures add column if not exists venue text;` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
4. `alter table public.training_sessions add column if not exists venue text;` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
5. `alter table public.volunteer_shifts add column if not exists venue text;` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
6. `update public.teams t set home_venue=coalesce(t.home_venue,nullif(concat_ws(', ',nullif(v.name,''),nullif(v.suburb,'')),'')) from public.venues v where v.id=t.home_venue_id;` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
7. `update public.teams t set training_venue=coalesce(t.training_venue,nullif(concat_ws(', ',nullif(v.name,''),nullif(v.suburb,'')),'')) from public.venues v where v.id=t.training_venu…` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
8. `update public.club_events e set venue=coalesce(e.venue,nullif(concat_ws(', ',nullif(v.name,''),nullif(v.suburb,'')),'')) from public.venues v where v.id=e.venue_id;` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
9. `update public.fixtures f set venue=coalesce(f.venue,nullif(concat_ws(', ',nullif(v.name,''),nullif(v.suburb,'')),'')) from public.venues v where v.id=f.venue_id;` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
10. `update public.training_sessions s set venue=coalesce(s.venue,nullif(concat_ws(', ',nullif(v.name,''),nullif(v.suburb,'')),'')) from public.venues v where v.id=s.venue_id;` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
11. `update public.volunteer_shifts s set venue=coalesce(s.venue,nullif(concat_ws(', ',nullif(v.name,''),nullif(v.suburb,'')),'')) from public.venues v where v.id=s.venue_id;` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
12. `alter table public.teams drop constraint if exists teams_home_venue_length, drop constraint if exists teams_training_venue_length;` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
13. `alter table public.teams add constraint teams_home_venue_length check(home_venue is null or char_length(home_venue)<=240), add constraint teams_training_venue_length check(training…` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
14. `alter table public.club_events drop constraint if exists club_events_venue_length;` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
15. `alter table public.club_events add constraint club_events_venue_length check(venue is null or char_length(venue)<=240);` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
16. `alter table public.fixtures drop constraint if exists fixtures_venue_length;` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
17. `alter table public.fixtures add constraint fixtures_venue_length check(venue is null or char_length(venue)<=240);` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
18. `alter table public.training_sessions drop constraint if exists training_sessions_venue_length;` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
19. `alter table public.training_sessions add constraint training_sessions_venue_length check(venue is null or char_length(venue)<=240);` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
20. `alter table public.volunteer_shifts drop constraint if exists volunteer_shifts_venue_length;` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
21. `alter table public.volunteer_shifts add constraint volunteer_shifts_venue_length check(venue is null or char_length(venue)<=240);` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
22. `alter table public.teams drop column if exists home_venue_id, drop column if exists training_venue_id;` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
23. `alter table public.club_events drop column if exists venue_id;` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
24. `alter table public.fixtures drop column if exists venue_id;` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
25. `alter table public.training_sessions drop column if exists venue_id;` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
26. `alter table public.volunteer_shifts drop column if exists venue_id;` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
27. `alter table public.canteen_orders drop column if exists venue_id;` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
28. `alter table public.voucher_issuances drop column if exists venue_id;` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
29. `alter table public.voucher_redemptions drop column if exists venue_id;` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
30. `drop table if exists public.canteen_venues;` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
31. `drop table if exists public.venues;` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
32. `create or replace function app_private.redeem_voucher( redemption_token text, redeem_venue_id uuid, redeem_amount_cents int, redeem_order_id uuid default null, device_label text de…` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
33. `create or replace function app_private.create_canteen_order( target_product_id uuid, target_venue_id uuid default null, target_beneficiary_id uuid default null, order_quantity int …` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
34. `create or replace function public.checkout_canteen_cart( request_key text, target_wallet_id uuid default null, target_wallet_cents int default 0, target_voucher_ids uuid[] default …` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)
35. `` — **partially applied manually** (Location text support is present in places, but venues and canteen_venues still exist; destructive drops were not applied.)

### 20260727111513_simplify_team_fields

Classification: **partially applied manually**. The age_groups table still exists, so the two-statement simplification was not completed.

1. `alter table public.teams drop column if exists age_group_id, drop column if exists home_venue, drop column if exists training_venue;` — **partially applied manually** (The age_groups table still exists, so the two-statement simplification was not completed.)
2. `drop table if exists public.age_groups;` — **partially applied manually** (The age_groups table still exists, so the two-statement simplification was not completed.)

### 20260728021848_wwcc_submission_workflow

Classification: **not applied**. wwcc_submissions, its functions, policies, indexes and trigger are absent.

1. `insert into public.roles( key,name,description,is_system,is_sensitive,may_request, requires_team_scope,requires_season_scope,requires_super_admin_approval, sort_order,is_active,rol…` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
2. `create table if not exists public.wwcc_submissions ( id uuid primary key default gen_random_uuid(), user_id uuid not null references public.profiles(id) on delete cascade, role_ass…` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
3. `alter table public.wwcc_submissions enable row level security;` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
4. `create unique index if not exists wwcc_submissions_one_pending_per_user on public.wwcc_submissions(user_id) where status='pending';` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
5. `create index if not exists wwcc_submissions_user_history_idx on public.wwcc_submissions(user_id,submitted_at desc);` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
6. `create index if not exists wwcc_submissions_review_queue_idx on public.wwcc_submissions(status,submitted_at);` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
7. `create index if not exists wwcc_submissions_expiry_idx on public.wwcc_submissions(expiry_date) where status='approved';` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
8. `drop trigger if exists wwcc_submissions_set_updated_at on public.wwcc_submissions;` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
9. `create trigger wwcc_submissions_set_updated_at before update on public.wwcc_submissions for each row execute function app_private.set_updated_at();` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
10. `revoke all on public.wwcc_submissions from anon,authenticated;` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
11. `grant select,insert on public.wwcc_submissions to authenticated;` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
12. `grant select,insert,update,delete on public.wwcc_submissions to service_role;` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
13. `drop policy if exists wwcc_submissions_read on public.wwcc_submissions;` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
14. `create policy wwcc_submissions_read on public.wwcc_submissions for select to authenticated using ( user_id=(select auth.uid()) or app_private.has_permission('volunteers.view') or a…` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
15. `drop policy if exists wwcc_submissions_insert_own on public.wwcc_submissions;` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
16. `create policy wwcc_submissions_insert_own on public.wwcc_submissions for insert to authenticated with check ( user_id=(select auth.uid()) and status='pending' and reviewed_by is nu…` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
17. `create or replace function public.assign_user_role( target_user_id uuid, target_role_id uuid, target_team_id uuid default null, target_season_id uuid default null, starts_at timest…` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
18. `revoke all on function public.assign_user_role(uuid,uuid,uuid,uuid,timestamptz,timestamptz,text) from public,anon;` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
19. `grant execute on function public.assign_user_role(uuid,uuid,uuid,uuid,timestamptz,timestamptz,text) to authenticated,service_role;` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
20. `drop index if exists public.user_role_assignments_one_current_volunteer;` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
21. `create unique index user_role_assignments_one_current_volunteer on public.user_role_assignments(user_id,role_id) where revoked_at is null and status in ('pending','active','suspend…` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
22. `create or replace function public.submit_wwcc_submission( submission_id uuid, assignment_id uuid, legal_name text, wwcc_number text, expiry_date date, document_file_id uuid, submis…` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
23. `revoke all on function public.submit_wwcc_submission(uuid,uuid,text,text,date,uuid,text) from public,anon;` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
24. `grant execute on function public.submit_wwcc_submission(uuid,uuid,text,text,date,uuid,text) to authenticated,service_role;` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
25. `create or replace function public.review_wwcc_submission( submission_id uuid, decision text, decision_reason text, corrected_expiry_date date default null, corrected_wwcc_number te…` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
26. `revoke all on function public.review_wwcc_submission(uuid,text,text,date,text) from public,anon;` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
27. `grant execute on function public.review_wwcc_submission(uuid,text,text,date,text) to authenticated,service_role;` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
28. `create or replace function public.refresh_wwcc_status(target_user_id uuid default null) returns integer language plpgsql security definer set search_path='' as $$ declare actor uui…` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
29. `revoke all on function public.refresh_wwcc_status(uuid) from public,anon;` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
30. `grant execute on function public.refresh_wwcc_status(uuid) to authenticated,service_role;` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
31. `revoke execute on function public.update_member_compliance(uuid,text,text,text,text,text,date,text,text) from authenticated;` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
32. `revoke execute on function public.request_volunteer_shift(uuid) from authenticated;` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
33. `revoke execute on function public.update_volunteer_assignment(uuid,text,text) from authenticated;` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
34. `revoke execute on function public.update_volunteer_shift_status(uuid,text,text) from authenticated;` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
35. `comment on table public.volunteer_opportunities is 'Legacy volunteer recruitment data retained for controlled cleanup; no longer used by the application.';` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
36. `comment on table public.volunteer_shifts is 'Legacy volunteer roster data retained for controlled cleanup; no longer used by the application.';` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)
37. `comment on table public.volunteer_assignments is 'Legacy volunteer roster history retained for controlled cleanup; no longer used by the application.';` — **not applied** (wwcc_submissions, its functions, policies, indexes and trigger are absent.)

### 20260728120000_member_portal_workflows

Classification: **not applied**. Its volunteer/family/team-board corrective objects are absent and it depends on the unapplied WWCC migration.

1. `alter table if exists public.wwcc_submissions add column if not exists date_of_birth date, add column if not exists clearance_type text, add column if not exists verification_outco…` — **not applied** (Its volunteer/family/team-board corrective objects are absent and it depends on the unapplied WWCC migration.)
2. `do $$ begin alter table public.wwcc_submissions add constraint wwcc_submissions_clearance_type_check check (clearance_type in ('volunteer','paid_worker')); exception when duplicate…` — **not applied** (Its volunteer/family/team-board corrective objects are absent and it depends on the unapplied WWCC migration.)
3. `do $$ begin alter table public.wwcc_submissions add constraint wwcc_submissions_verified_clearance_type_check check (verified_clearance_type is null or verified_clearance_type in (…` — **not applied** (Its volunteer/family/team-board corrective objects are absent and it depends on the unapplied WWCC migration.)
4. `update public.roles set may_request=true, description='Member-requested volunteer access that activates only after authorised external WWCC verification.' where key='volunteer';` — **not applied** (Its volunteer/family/team-board corrective objects are absent and it depends on the unapplied WWCC migration.)
5. `create or replace function public.request_volunteer_role() returns uuid language plpgsql security definer set search_path='' as $$ declare actor uuid=(select auth.uid()); volunteer…` — **not applied** (Its volunteer/family/team-board corrective objects are absent and it depends on the unapplied WWCC migration.)
6. `revoke all on function public.request_volunteer_role() from public,anon;` — **not applied** (Its volunteer/family/team-board corrective objects are absent and it depends on the unapplied WWCC migration.)
7. `grant execute on function public.request_volunteer_role() to authenticated,service_role;` — **not applied** (Its volunteer/family/team-board corrective objects are absent and it depends on the unapplied WWCC migration.)
8. `create or replace function public.create_family_group(group_name text) returns uuid language plpgsql security definer set search_path='' as $$ declare actor uuid=(select auth.uid()…` — **not applied** (Its volunteer/family/team-board corrective objects are absent and it depends on the unapplied WWCC migration.)
9. `revoke all on function public.create_family_group(text) from public,anon;` — **not applied** (Its volunteer/family/team-board corrective objects are absent and it depends on the unapplied WWCC migration.)
10. `grant execute on function public.create_family_group(text) to authenticated,service_role;` — **not applied** (Its volunteer/family/team-board corrective objects are absent and it depends on the unapplied WWCC migration.)
11. `alter table public.team_posts add column if not exists last_edited_at timestamptz, add column if not exists last_edited_by uuid references public.profiles(id) on delete set null, a…` — **not applied** (Its volunteer/family/team-board corrective objects are absent and it depends on the unapplied WWCC migration.)
12. `alter table public.match_reports add column if not exists match_description text, add column if not exists match_date date, add column if not exists venue text, add column if not e…` — **not applied** (Its volunteer/family/team-board corrective objects are absent and it depends on the unapplied WWCC migration.)
13. `create unique index if not exists team_post_one_like_per_user on public.team_post_reactions(post_id,user_id);` — **not applied** (Its volunteer/family/team-board corrective objects are absent and it depends on the unapplied WWCC migration.)
14. `create unique index if not exists team_poll_one_response_per_user on public.team_poll_responses(post_id,user_id);` — **not applied** (Its volunteer/family/team-board corrective objects are absent and it depends on the unapplied WWCC migration.)
15. `create index if not exists team_posts_visible_feed_idx on public.team_posts(team_id,published_at desc) where status='active' and deleted_at is null;` — **not applied** (Its volunteer/family/team-board corrective objects are absent and it depends on the unapplied WWCC migration.)
16. `create index if not exists match_reports_visible_idx on public.match_reports(team_id,created_at desc) where deleted_at is null;` — **not applied** (Its volunteer/family/team-board corrective objects are absent and it depends on the unapplied WWCC migration.)
17. `alter table public.team_post_reactions drop constraint if exists team_post_reactions_reaction_check;` — **not applied** (Its volunteer/family/team-board corrective objects are absent and it depends on the unapplied WWCC migration.)
18. `alter table public.team_post_reactions add constraint team_post_reactions_reaction_check check (reaction='like');` — **not applied** (Its volunteer/family/team-board corrective objects are absent and it depends on the unapplied WWCC migration.)
19. `drop policy if exists team_post_reactions_own_delete on public.team_post_reactions;` — **not applied** (Its volunteer/family/team-board corrective objects are absent and it depends on the unapplied WWCC migration.)
20. `create policy team_post_reactions_own_delete on public.team_post_reactions for delete to authenticated using (user_id=(select auth.uid()));` — **not applied** (Its volunteer/family/team-board corrective objects are absent and it depends on the unapplied WWCC migration.)
21. `grant delete on public.team_post_reactions to authenticated;` — **not applied** (Its volunteer/family/team-board corrective objects are absent and it depends on the unapplied WWCC migration.)
22. `drop policy if exists wwcc_submissions_insert_own on public.wwcc_submissions;` — **not applied** (Its volunteer/family/team-board corrective objects are absent and it depends on the unapplied WWCC migration.)
23. `create policy wwcc_submissions_insert_own on public.wwcc_submissions for insert to authenticated with check ( user_id=(select auth.uid()) and status='pending' and reviewed_by is nu…` — **not applied** (Its volunteer/family/team-board corrective objects are absent and it depends on the unapplied WWCC migration.)
24. `drop policy if exists match_reports_team_read on public.match_reports;` — **not applied** (Its volunteer/family/team-board corrective objects are absent and it depends on the unapplied WWCC migration.)
25. `create policy match_reports_team_read on public.match_reports for select to authenticated using (deleted_at is null and app_private.can_access_team(team_id));` — **not applied** (Its volunteer/family/team-board corrective objects are absent and it depends on the unapplied WWCC migration.)

## Production observations

- Present: the expected legacy team, family, wallet, canteen, merchandise, notification and content tables; their sampled policies, triggers and indexes are also present.
- Absent: `member_compliance`, `wwcc_submissions`, WWCC functions/policies/indexes/trigger, and the new member-portal volunteer/family corrective functions.
- Still present: `venues`, `canteen_venues`, and `age_groups`; therefore the destructive simplification migrations were not fully applied.
- Constraint drift: `wallet_qr_tokens` uses a `(wallet_account_id, status)` unique constraint rather than the local wallet-only constraint. `voucher_templates.code` is not backed by the expected named unique constraint.

## Safe next actions

1. Authenticate the Supabase CLI.
2. Repair only the 14 proven equivalent local timestamps with `supabase migration repair <timestamp> --status applied`; do not rerun their SQL.
3. Diff the three non-equivalent alternate migrations against the recorded production bodies before deciding whether a corrective migration is needed.
4. Create forward-only corrective migrations for the missing compliance/WWCC/member-portal statements; do not mark those source migrations applied first.
5. Re-run migration history and RLS/database tests before deployment.

## Corrective deployment addendum — 2026-07-29

The 14 proven-equivalent timestamp repairs were completed by the repository owner before this continuation and were not repeated.

Applied through official `supabase migration up --linked` tooling:

- `20260729063114_complete_member_roles_compliance_foundation.sql`
- `20260729063140_add_final_wwcc_workflow.sql`
- `20260729063223_add_final_member_portal_workflows.sql`
- `20260729064646_harden_wwcc_assignment_rls.sql` (required by the post-apply role-matrix test; preserves roles RLS while allowing owner WWCC submission)

Production verification confirmed the compliance and WWCC tables, RLS, policies, grants, indexes, triggers, RPCs, edit metadata, unique like/poll indexes, optional private-document reference, self-approval rejection, authorised review, role activation, child-account rejection, and unrelated-user WWCC isolation. `venues`, `canteen_venues`, and `age_groups` remain untouched.

The 13 uncertain early migrations, three differing-SQL migrations, and original partial/unapplied migration timestamps remain unrepaired and were not executed.
